'****************************************************************************************
'Generated By: MakeSFAR V.2.0.0 - 컨트롤 클래스 메이커 - 한화 S&C
'시스템구분    : 솔루션명 /시스템명/Server Control Class
'실행   환경    : COM+ Service Server Package
'프로그램명    : ccMDCMREALMEDCODEMST.vb
'기         능    : - 기능을 명시 합니다.
'특이  사항     : - 특이사항에 대해 표현
'                     -
'----------------------------------------------------------------------------------------
'HISTORY    :1) 2004-03-30 오전 10:32:13 By MakeSFARV.2.0.0
'                  2) 2004-03-30 오전 10:32:13 By 작성자명을 씁니다.
'****************************************************************************************

Imports System.Xml                        ' XML처리
Imports SCGLControl                       ' ControlClass의 Base Class
Imports SCGLUtil.cbSCGLConfig       ' ConfigurationClass
Imports SCGLUtil.cbSCGLErr            '오류처리 클래스
Imports SCGLUtil.cbSCGLXml           'XML처리 클래스
Imports SCGLUtil.cbSCGLUtil            '기타유틸리티 클래스
Imports eMDCO


' 엔티티 클래스 사용시 해당 엔티티 클래스의 프로젝트를 참조한 후 Imports 하십시요. 
' Imports 엔티티프로젝트

Public Class ccMDOTCLOUDCUST
    Inherits ccControl

#Region "GROUP BLOCK : 전역 또는 모듈레벨의 변수/상수 선언"
    Private CLASS_NAME = "ccMDOTCLOUDCUST"                  '자신의 클래스명
    Private mobjceMD_CLOUD_CUST As eMDCO.ceMD_CLOUD_CUST
#End Region

#Region "GROUP BLOCK : Event 선언"
#End Region

#Region "GROUP BLOCK : 외부에 공개 Method"

    ' =============== 코바코 매체사 코드 조회
    Public Function SelectRtn(ByVal strInfoXML As String, _
                              ByRef intRowCnt As Integer, _
                              ByRef intColCnt As Integer, _
                              ByVal strMEDNAME As String, _
                              ByVal strBUSINO As String, _
                              ByVal strREAL_MED_NAME As String, _
                              ByVal strUSE_FLAG As String) As Object


        Dim strWhere As String       'Where조건 변수
        Dim strFormat As String      'SQL Format 변수
        Dim strSQL As String          'SQL 변수
        Dim vntData As Object        'Array Return 변수(Array 를사용할 때 선언)
        Dim Con1, Con2, Con3, Con4 As String
        
        With mobjSCGLConfig '기본정보 Config 개체
            Try
                SetConfig(strInfoXML) '기본정보 Setting

                Con1 = "" : Con2 = "" : Con3 = "" : Con4 = ""

                If strMEDNAME <> "" Then Con1 = String.Format(" AND (DBO.SC_GET_HIGHCUSTNAME_FUN(MEDCODE) like '%{0}%')", strMEDNAME)
                If strBUSINO <> "" Then Con2 = String.Format(" AND (DBO.SC_GET_BUSINO_FUN(MEDCODE) LIKE '%{0}%')", strBUSINO)
                If strREAL_MED_NAME <> "" Then Con3 = String.Format(" AND (DBO.SC_GET_HIGHCUSTNAME_FUN(REAL_MED_CODE) LIKE '%{0}%')", strREAL_MED_NAME)
                If strUSE_FLAG <> "" Then Con4 = String.Format(" AND (USE_FLAG = '{0}')", strUSE_FLAG)

                strWhere = BuildFields(" ", Con1, Con2, Con3, Con4)

                strFormat = " select "
                strFormat = strFormat & " SEQ, "
                strFormat = strFormat & " MEDCODE,"
                strFormat = strFormat & " DBO.SC_GET_HIGHCUSTNAME_FUN(MEDCODE) MEDNAME,"
                strFormat = strFormat & " REAL_MED_CODE,"
                strFormat = strFormat & " DBO.SC_GET_HIGHCUSTNAME_FUN(REAL_MED_CODE) REAL_MED_NAME, USE_FLAG"
                strFormat = strFormat & " from MD_CLOUD_CUST "
                strFormat = strFormat & " where 1=1 {0}"
                strFormat = strFormat & " ORDER BY SEQ "

                strSQL = String.Format(strFormat, strWhere)

                ' DB 접속
                .mobjSCGLSql.SQLConnect(.DBConnStr)
                vntData = .mobjSCGLSql.SQLSelectArr(strSQL, intRowCnt, intColCnt, , True)
                Return vntData
            Catch err As Exception
                ' 오류 전송
                Throw RaiseSysErr(err, CLASS_NAME & ".SelectRtn")
            Finally
                ' DB 접속 종료
                .mobjSCGLSql.SQLDisconnect()
            End Try
        End With
    End Function

    '============== 코바코 매체코드 등록 저장
    Public Function ProcessRtn(ByVal strInfoXML As String, _
                               ByVal vntData As Object) As Integer

        Dim intRtn As Integer
        Dim i, intColCnt, intRows As Integer
        Dim dblID As Double '자동 ID 를사용할 때만 사용
        Dim strBCODE As String
        Dim strCUSTCODE As String
        Dim strBRODCODE As String
        Dim strMEDIUMCODE As String
        Dim strOFFICECODE As String

        SetConfig(strInfoXML)
        With mobjSCGLConfig
            Try
                .mobjSCGLSql.SQLConnect(.DBConnStr)
                .mobjSCGLSql.SQLBeginTrans()

                If IsArray(vntData) Then
                    '''사용할 Entity 개체생성(Config 정보를 넘겨생성)
                    mobjceMD_CLOUD_CUST = New ceMD_CLOUD_CUST(mobjSCGLConfig)
                    '''vntData의 컬럼수, 로우수를 변수입력
                    intColCnt = UBound(vntData, 1) : intRows = UBound(vntData, 2)
                    '''해당하는Row 만큼 Loop
                    strBCODE = ""
                    strCUSTCODE = ""
                    For i = 1 To intRows
                        '신규일때
                        If GetElement(vntData, "SEQ", intColCnt, i, OPTIONAL_STR) = "" Then
                            intRtn = InsertRtn_CLOUD_CUST(vntData, intColCnt, i)
                        Else
                            intRtn = UpdateRtn_CLOUD_CUST(vntData, intColCnt, i)
                        End If
                    Next
                End If
                .mobjSCGLSql.SQLCommitTrans()
                Return intRows
            Catch err As Exception
                .mobjSCGLSql.SQLRollbackTrans()
                Throw RaiseSysErr(err, CLASS_NAME & ".ProcessRtn")
            Finally
                .mobjSCGLSql.SQLDisconnect()
                mobjceMD_CLOUD_CUST.Dispose()
            End Try
        End With
    End Function
#End Region

#Region "GROUP BLOCK : 외부에 비공개 Method"

    Private Function InsertRtn_CLOUD_CUST(ByVal vntData As Object, _
                                          ByVal intColCnt As Integer, _
                                          ByVal intRow As Integer) As Integer
        Dim intRtn As Integer
        intRtn = mobjceMD_CLOUD_CUST.InsertDo( _
                                       GetElement(vntData, "MEDCODE", intColCnt, intRow), _
                                       GetElement(vntData, "REAL_MED_CODE", intColCnt, intRow), _
                                       GetElement(vntData, "USE_FLAG", intColCnt, intRow))
        Return intRtn
    End Function

    Private Function UpdateRtn_CLOUD_CUST(ByVal vntData As Object, _
                                          ByVal intColCnt As Integer, _
                                          ByVal intRow As Integer) As Integer
        Dim intRtn As Integer
        intRtn = mobjceMD_CLOUD_CUST.UpdateDo( _
                                       GetElement(vntData, "SEQ", intColCnt, intRow), _
                                       GetElement(vntData, "MEDCODE", intColCnt, intRow), _
                                       GetElement(vntData, "REAL_MED_CODE", intColCnt, intRow), _
                                       GetElement(vntData, "USE_FLAG", intColCnt, intRow))
        Return intRtn
    End Function

#End Region
End Class