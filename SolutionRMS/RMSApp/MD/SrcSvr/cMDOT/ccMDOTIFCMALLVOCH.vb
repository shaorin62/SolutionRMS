'****************************************************************************************
'Generated By: MakeSFAR V.2.0.0 - 컨트롤 클래스 메이커
'시스템구분    : 솔루션명 /시스템명/Server Control Class
'실행   환경    : COM+ Service Server Package
'프로그램명    : ccMDCMDEPTMST.vb
'기         능    : - 기능을 명시 합니다.
'특이  사항     : - 특이사항에 대해 표현
'----------------------------------------------------------------------------------------
'HISTORY    :1)
'            2)
'****************************************************************************************

Imports System.Xml                  ' XML처리
Imports SCGLControl                 ' ControlClass의 Base Class
Imports SCGLUtil.cbSCGLConfig       ' ConfigurationClass
Imports SCGLUtil.cbSCGLErr          '오류처리 클래스
Imports SCGLUtil.cbSCGLXml          'XML처리 클래스
Imports SCGLUtil.cbSCGLUtil         '기타유틸리티 클래스
Imports eMDCO                       '엔터티 추가

' 엔티티 클래스 사용시 해당 엔티티 클래스의 프로젝트를 참조한 후 Imports 하십시요. 
' Imports 엔티티프로젝트

Public Class ccMDOTIFCMALLVOCH
    Inherits ccControl

#Region "GROUP BLOCK : 전역 또는 모듈레벨의 변수/상수 선언"
    Private CLASS_NAME = "ccMDOTIFCMALLVOCH"                  '자신의 클래스명
    Private mobjceMD_TRUVOCH_MST As eMDCO.ceMD_TRUVOCH_MST             '사용할 Entity 변수 선언
#End Region

#Region "GROUP BLOCK : 외부에비공개"
    Public Function Get_COMBO_VALUE(ByVal strInfoXML As String, _
                                    ByRef intRowCnt As Integer, _
                                    ByRef intColCnt As Integer, _
                                    ByVal strCODE As String) As Object

        Dim strSQL, strFormat, strSelFields As String
        Dim vntData As Object

        SetConfig(strInfoXML)   '기본정보 설정					

        '조회 필드 설정					
        strSelFields = "CODE, CODE_NAME"

        'SQL문 생성
        strFormat = "SELECT {0} " & _
                    "FROM SC_CODE " & _
                    "WHERE CLASS_CODE = '" & strCODE & "' and use_yn = 'Y'" & _
                    "ORDER BY SORT_SEQ "

        With mobjSCGLConfig
            strSQL = String.Format(strFormat, strSelFields)

            ''데이터 조회
            Try
                .mobjSCGLSql.SQLConnect(.DBConnStr)
                vntData = .mobjSCGLSql.SQLSelectArr(strSQL, intRowCnt, intColCnt)
                Return vntData
            Catch err As Exception
                Throw RaiseSysErr(err, CLASS_NAME & ".Get_COMBO_VALUE")
            Finally
                .mobjSCGLSql.SQLDisconnect()
            End Try
        End With
    End Function

    Public Function Get_SUBCOMBO_VALUE(ByVal strInfoXML As String, _
                                   ByRef intRowCnt As Integer, _
                                   ByRef intColCnt As Integer, _
                                   ByVal strCODE As String) As Object

        Dim strSQL, strFormat As String
        Dim vntData As Object

        SetConfig(strInfoXML)   '기본정보 설정					

        '조회 필드 설정					
        strFormat = " SELECT "
        strFormat = strFormat & " BANK_TYPE CODE, "
        strFormat = strFormat & " BANK_TYPE CODE_NAME "
        strFormat = strFormat & " FROM SC_BANKTYPE_MST"
        strFormat = strFormat & " WHERE 1=1"
        strFormat = strFormat & " AND REPLACE(BUSINO,'-','') = '" & strCODE & "'"
        strFormat = strFormat & " AND USE_YN = 'Y'"

        With mobjSCGLConfig
            strSQL = String.Format(strFormat)

            ''데이터 조회
            Try
                .mobjSCGLSql.SQLConnect(.DBConnStr)
                vntData = .mobjSCGLSql.SQLSelectArr(strSQL, intRowCnt, intColCnt)
                Return vntData
            Catch err As Exception
                Throw RaiseSysErr(err, CLASS_NAME & ".Get_SUBCOMBO_VALUE")
            Finally
                .mobjSCGLSql.SQLDisconnect()
            End Try
        End With
    End Function

    Public Function SelectRtn_SUSU(ByVal strInfoXML As String, _
                                   ByRef intRowCnt As Integer, _
                                   ByRef intColCnt As Integer, _
                                   ByVal strYEARMON As String, _
                                   ByVal strCLIENTCODE As String, _
                                   ByVal strCLIENTNAME As String, _
                                   ByVal strGBN As String) As Object

        Dim strSQL, strFormat, strWhere As String
        Dim vntData As Object
        Dim Con1, Con2, Con3, Con4, Con5, Con6, Con7, Con8 As String

        Con1 = "" : Con2 = "" : Con3 = "" : Con4 = "" : Con5 = "" : Con6 = "" : Con7 = "" : Con8 = ""

        SetConfig(strInfoXML)   '기본정보 설정
        With mobjSCGLConfig

            If strYEARMON <> "" Then Con1 = String.Format(" AND (A.TAXYEARMON = '{0}')", strYEARMON)
            If strCLIENTCODE <> "" Then Con2 = String.Format(" AND A.CLIENTCODE = '{0}'", strCLIENTCODE)
            If strCLIENTNAME <> "" Then Con3 = String.Format(" AND DBO.SC_GET_HIGHCUSTNAME_FUN(A.CLIENTCODE)  LIKE '%{0}%'", strCLIENTNAME)

            If strGBN <> "" Then
                If strGBN = "rdT" Then
                    Con6 = " AND ISNULL(A.VOCHNO,'') <> '' "
                ElseIf strGBN = "rdF" Then
                    Con6 = " AND ISNULL(A.VOCHNO,'') = '' AND ISNULL(B.ERRMSG,'') = '' "
                ElseIf strGBN = "rdE" Then
                    Con6 = " AND ISNULL(B.ERRMSG,'') <> '' "
                ElseIf strGBN = "rdA" Then
                    Con6 = " "
                End If
            End If

            '조회 필드 설정
            strWhere = BuildFields(" ", Con1, Con2, Con3, Con4, Con5, Con6, Con7, Con8)

            strFormat = strFormat & " SELECT "
            strFormat = strFormat & " 0 chk, "
            strFormat = strFormat & " '1' GFLAG, "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then A.DEMANDDAY else b.postingdate end as postingdate,"
            strFormat = strFormat & " replace(A.REAL_MED_BISNO,'-','') customercode,"
            strFormat = strFormat & " REAL_MED_NAME CUSTNAME,"
            strFormat = strFormat & " case isnull(b.summ,'') when '' then SUBSTRING(A.TAXYEARMON,5,2) +'월IFC MALL 광고비'  else b.summ end as  SUMM, "
            strFormat = strFormat & " case when isnull(A.AMT,0) < 0 then abs(isnull(A.AMT,0)) else  dbo.lpad(A.AMT,13,'0') end  amt, "
            strFormat = strFormat & " case isnull(b.vat,0) when 0 then case when isnull(A.vat,0) < 0 then abs(isnull(A.vat,0)) else dbo.lpad(A.vat,13,'0') end else isnull(b.vat,0) end vat, "
            strFormat = strFormat & " CASE isnull(b.semu,'') when '' then 'B0' else b.semu end as SEMU,"
            strFormat = strFormat & " CASE isnull(b.BP,'') when '' then '8000' else b.BP end as BP,"
            strFormat = strFormat & " CASE ISNULL(B.DEMANDDAY,'') WHEN '' THEN CONVERT(CHAR(8),DATEADD(DD,60,replace(CONVERT(VARCHAR(10), dateadd(d,-day(dateadd(m,1,a.demandday)), dateadd(m,1, a.demandday  )),121 ) ,'-','') ),112) ELSE B.DEMANDDAY END AS DEMANDDAY, "
            strFormat = strFormat & " CASE ISNULL(B.DUEDATE,'') WHEN '' THEN CONVERT(CHAR(8),DATEADD(DD,60,A.DEMANDDAY ),112) ELSE B.DUEDATE END AS DUEDATE, "
            strFormat = strFormat & " replace(A.REAL_MED_BISNO,'-','') VENDOR,"
            strFormat = strFormat & " '' REAL_MED_NAME,"
            strFormat = strFormat & " 'S' gbn,"
            strFormat = strFormat & " A.DEMANDDAY documentdate,"
            strFormat = strFormat & " CASE isnull(b.prepayment,'') when '' then '' else b.prepayment end as  prepayment,"
            strFormat = strFormat & " CASE isnull(b.prepayment,'') when '' then '' else b.fromdate end as  fromdate,"
            strFormat = strFormat & " CASE isnull(b.prepayment,'') when '' then '' else b.todate end as  todate,"
            strFormat = strFormat & " CASE ISNULL(B.SUMMTEXT,'') WHEN '' THEN SUBSTRING(A.TAXYEARMON,5,2) +'월IFC MALL 광고비' ELSE B.SUMMTEXT END AS summtext,"
            strFormat = strFormat & " case when isnull(A.AMT,0) < 0 then '-' else '+' end AMTGBN,"
            strFormat = strFormat & " A.TAXYEARMON,"
            strFormat = strFormat & " A.TAXNO,"
            strFormat = strFormat & " CASE isnull(A.VOCHNO,'') WHEN '' THEN '' ELSE A.VOCHNO END VOCHNO,"
            strFormat = strFormat & " B.RMSNO,"
            strFormat = strFormat & " B.ERRCODE,"
            strFormat = strFormat & " B.ERRMSG, "
            strFormat = strFormat & " A.MEDFLAG "
            strFormat = strFormat & " FROM MD_COMMITAX_HDR  A "
            strFormat = strFormat & " LEFT JOIN MD_TRUVOCH_MST B "
            strFormat = strFormat & " ON A.TAXYEARMON = B.TAXYEARMON AND A.TAXNO = B.TAXNO And B.GBN = 'S' AND B.MEDFLAG = 'Y' "
            strFormat = strFormat & " WHERE A.MEDFLAG IN ('Y') "
            strFormat = strFormat & " and isnull(A.attr10,0) <> 999999"
            strFormat = strFormat & " and isnull(A.MERGEFLAG,'') <> '1'  AND ISNULL(B.DEBTOR,'') = '' "
            strFormat = strFormat & " AND ISNULL(A.ATTR01,0) <> 1 "
            strFormat = strFormat & " {0} "


            strSQL = String.Format(strFormat, strWhere)

            '데이터 조회
            Try
                .mobjSCGLSql.SQLConnect(.DBConnStr)
                vntData = .mobjSCGLSql.SQLSelectArr(strSQL, intRowCnt, intColCnt, , True)
                Return vntData
            Catch err As Exception
                Throw RaiseSysErr(err, CLASS_NAME & ".SelectRtn")
            Finally
                .mobjSCGLSql.SQLDisconnect()
            End Try
        End With
    End Function

    Public Function SelectRtn_SUSUDTL(ByVal strInfoXML As String, _
                                      ByRef intRowCnt As Integer, _
                                      ByRef intColCnt As Integer, _
                                      ByVal strTAXYEARMON As String, _
                                      ByVal strTAXNO As String, _
                                      ByVal strPREPAYMENT As String) As Object

        Dim strSQL As String            'SQL문 
        Dim strFormat As String         '임시 SQL문
        Dim strWhere As String
        Dim vntData As Object
        Dim Con1, Con2 As String

        SetConfig(strInfoXML)   '기본정보 설정

        Con1 = "" : Con2 = ""

        If strTAXYEARMON <> "" Then Con1 = String.Format(" AND (B.TAXYEARMON = '{0}')", strTAXYEARMON)
        If strTAXNO <> "" Then Con2 = String.Format(" AND (B.TAXNO = '{0}')", strTAXNO)

        strWhere = BuildFields(" ", Con1, Con2)

        strFormat = " SELECT  "
        strFormat = strFormat & " '1' SORT,"
        strFormat = strFormat & " '1' GFLAG,  "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then B.DEMANDDAY else C.postingdate end as postingdate,"
        strFormat = strFormat & " replace(B.REAL_MED_BISNO,'-','') customercode, "
        strFormat = strFormat & " REAL_MED_NAME CUSTNAME, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then SUBSTRING(B.TAXYEARMON,5,2) +'월 IFC MALL 광고비'  else C.summ end as  SUMM,  "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then dbo.SC_GET_BA(B.DEPT_CD) else C.ba end as BA,"
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then dbo.SC_GET_CCTR(B.DEPT_CD) else C.costcenter end as costcenter,"
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then case when isnull(B.AMT,0) < 0 then abs(isnull(B.AMT,0)) else dbo.lpad(B.AMT,13,'0') end ELSE ISNULL(C.SUMAMT,0) END amt,  "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then case when isnull(B.vat,0) < 0 then abs(isnull(B.vat,0)) else dbo.lpad(B.vat,13,'0') end else isnull(C.vat,0) end vat,  "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then 'B0' ELSE ISNULL(SEMU,'') END AS SEMU, "
        strFormat = strFormat & " '8000' BP, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then CONVERT(CHAR(8),DATEADD(DD,60,replace(CONVERT(VARCHAR(10), dateadd(d,-day(dateadd(m,1,B.demandday)), dateadd(m,1, B.demandday  )),121 ) ,'-','') ),112) ELSE C.DEMANDDAY END AS DEMANDDAY,  "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then CONVERT(CHAR(8),DATEADD(DD,60,B.DEMANDDAY ),112) ELSE C.DUEDATE END AS DUEDATE,  "
        strFormat = strFormat & " replace(B.REAL_MED_BISNO,'-','') VENDOR, "
        strFormat = strFormat & " '' REAL_MED_NAME, "
        strFormat = strFormat & " 'S' gbn, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then '200400' else C.account end as  ACCOUNT,  "
        strFormat = strFormat & " ''  DEBTOR,  "
        strFormat = strFormat & " CASE ISNULL(c.BMORDER,'') WHEN '' THEN 'AD0420' ELSE c.BMORDER END AS  BMORDER,"
        strFormat = strFormat & " CASE ISNULL(c.BANKTYPE,'') WHEN '' THEN '' ELSE c.BANKTYPE END AS  BANKTYPE,"
        strFormat = strFormat & " B.DEMANDDAY documentdate, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then '' else C.prepayment end as  prepayment, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then '' else C.fromdate end as  fromdate, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then '' else C.todate end as  todate, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then SUBSTRING(B.TAXYEARMON,5,2) +'월 IFC MALL 광고비' ELSE C.SUMMTEXT END AS summtext, "
        strFormat = strFormat & " case when isnull(B.AMT,0) < 0 then '-' else '+' end AMTGBN, "
        strFormat = strFormat & " B.TAXYEARMON, "
        strFormat = strFormat & " B.TAXNO, "
        strFormat = strFormat & " CASE isnull(B.VOCHNO,'') WHEN '' THEN '' ELSE B.VOCHNO END VOCHNO, "
        strFormat = strFormat & " C.RMSNO, "
        strFormat = strFormat & " C.ERRCODE, "
        strFormat = strFormat & " C.ERRMSG,  "
        strFormat = strFormat & " B.MEDFLAG  "
        strFormat = strFormat & " FROM MD_COMMITAX_HDR B "
        strFormat = strFormat & " LEFT JOIN MD_TRUVOCH_MST C  "
        strFormat = strFormat & " ON B.TAXYEARMON = C.TAXYEARMON AND B.TAXNO = C.TAXNO And C.GBN = 'S' "
        strFormat = strFormat & " WHERE B.MEDFLAG IN ('Y')  "
        strFormat = strFormat & " and isnull(B.attr10,0) <> 999999 "
        strFormat = strFormat & " and isnull(B.MERGEFLAG,'') <> '1'  AND ISNULL(C.DEBTOR,'') = '' "
        strFormat = strFormat & " {0} "
        strFormat = strFormat & " UNION ALL"
        strFormat = strFormat & " SELECT  "
        strFormat = strFormat & " '2' SORT,"
        strFormat = strFormat & " '1' GFLAG,  "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then B.DEMANDDAY else C.postingdate end as postingdate,"
        strFormat = strFormat & " replace(B.REAL_MED_BISNO,'-','') customercode, "
        strFormat = strFormat & " B.REAL_MED_NAME CUSTNAME, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then SUBSTRING(B.TAXYEARMON,5,2) +'월 IFC MALL 광고비'  else b.summ end as  SUMM,  "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then dbo.SC_GET_BA(A.DEPT_CD) else C.ba end as BA,"
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then dbo.SC_GET_CCTR(A.DEPT_CD) else C.costcenter end as costcenter,"
        strFormat = strFormat & " case when isnull(A.MC_AMT,0) < 0 then abs(isnull(A.MC_AMT,0)) else  dbo.lpad(A.MC_AMT,13,'0') end  amt,  "
        strFormat = strFormat & " 0 vat,  "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then 'B0' ELSE ISNULL(C.SEMU,'') END AS SEMU, "
        strFormat = strFormat & " '8000' BP, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then CONVERT(CHAR(8),DATEADD(DD,60,replace(CONVERT(VARCHAR(10), dateadd(d,-day(dateadd(m,1,B.demandday)), dateadd(m,1, B.demandday  )),121 ) ,'-','') ),112) ELSE C.DEMANDDAY END AS DEMANDDAY,  "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then CONVERT(CHAR(8),DATEADD(DD,60,B.DEMANDDAY ),112) ELSE C.DUEDATE END AS DUEDATE,  "
        strFormat = strFormat & " replace(B.REAL_MED_BISNO,'-','') VENDOR, "
        strFormat = strFormat & " '' REAL_MED_NAME, "
        strFormat = strFormat & " 'S' gbn, "
        strFormat = strFormat & " ''  ACCOUNT,  "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then '558830' else C.DEBTOR end as  DEBTOR,  "
        strFormat = strFormat & " CASE ISNULL(c.BMORDER,'') WHEN '' THEN 'AD0420' ELSE c.BMORDER END AS  BMORDER,"
        strFormat = strFormat & " CASE ISNULL(c.BANKTYPE,'') WHEN '' THEN '' ELSE c.BANKTYPE END AS  BANKTYPE,"
        strFormat = strFormat & " B.DEMANDDAY documentdate, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then '' else C.prepayment end as  prepayment, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then '' else C.fromdate end as  fromdate, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then '' else C.todate end as  todate, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then SUBSTRING(B.TAXYEARMON,5,2) +'월 IFC MALL 광고비' ELSE C.SUMMTEXT END AS summtext, "
        strFormat = strFormat & " case when isnull(A.AMT,0) < 0 then '-' else '+' end AMTGBN, "
        strFormat = strFormat & " B.TAXYEARMON, "
        strFormat = strFormat & " B.TAXNO, "
        strFormat = strFormat & " CASE ISNULL(B.VOCHNO,'') WHEN '' THEN '' ELSE B.VOCHNO END VOCHNO, "
        strFormat = strFormat & " C.RMSNO, "
        strFormat = strFormat & " C.ERRCODE, "
        strFormat = strFormat & " C.ERRMSG,  "
        strFormat = strFormat & " B.MEDFLAG  "
        strFormat = strFormat & " FROM MD_IFCMALL_AMT A  "
        strFormat = strFormat & " INNER JOIN MD_COMMITAX_HDR B ON A.COMMI_TAX_NO = B.TAXYEARMON + '-' + CAST(TAXNO AS VARCHAR(10))"
        strFormat = strFormat & " LEFT JOIN MD_TRUVOCH_MST C  "
        strFormat = strFormat & " ON A.COMMI_TAX_NO = C.TAXYEARMON + CAST(C.TAXNO AS VARCHAR(10))    And C.GBN = 'S'  "
        strFormat = strFormat & " WHERE B.MEDFLAG IN ('Y')  "
        strFormat = strFormat & " and isnull(B.attr10,0) <> 999999 "
        strFormat = strFormat & " and isnull(B.MERGEFLAG,'') <> '1' "
        strFormat = strFormat & " {0} "
        strFormat = strFormat & " UNION ALL"
        strFormat = strFormat & " SELECT  "
        strFormat = strFormat & " '3' SORT,"
        strFormat = strFormat & " '1' GFLAG,  "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then B.DEMANDDAY else C.postingdate end as postingdate,"
        strFormat = strFormat & " replace(B.REAL_MED_BISNO,'-','') customercode, "
        strFormat = strFormat & " B.REAL_MED_NAME CUSTNAME, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then SUBSTRING(B.TAXYEARMON,5,2) +'월 IFC MALL 광고비'  else b.summ end as  SUMM,  "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then dbo.SC_GET_BA(B.DEPT_CD) else C.ba end as BA,"
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then dbo.SC_GET_CCTR(B.DEPT_CD) else C.costcenter end as costcenter,"
        strFormat = strFormat & " case when isnull(A.EX_AMT,0) < 0 then abs(isnull(A.EX_AMT,0)) else  dbo.lpad(A.EX_AMT,13,'0') end  amt,  "
        strFormat = strFormat & " 0 vat,  "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then 'B0' ELSE ISNULL(C.SEMU,'') END AS SEMU, "
        strFormat = strFormat & " '8000' BP, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then CONVERT(CHAR(8),DATEADD(DD,60,replace(CONVERT(VARCHAR(10), dateadd(d,-day(dateadd(m,1,B.demandday)), dateadd(m,1, B.demandday  )),121 ) ,'-','') ),112) ELSE C.DEMANDDAY END AS DEMANDDAY,  "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then CONVERT(CHAR(8),DATEADD(DD,60,B.DEMANDDAY ),112) ELSE C.DUEDATE END AS DUEDATE,  "
        strFormat = strFormat & " replace(B.REAL_MED_BISNO,'-','') VENDOR, "
        strFormat = strFormat & " '' REAL_MED_NAME, "
        strFormat = strFormat & " 'S' gbn, "
        strFormat = strFormat & " ''  ACCOUNT,  "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then '558810' else C.DEBTOR end as  DEBTOR,  "
        strFormat = strFormat & " CASE ISNULL(c.BMORDER,'') WHEN '' THEN 'AD0420' ELSE c.BMORDER END AS  BMORDER,"
        strFormat = strFormat & " CASE ISNULL(c.BANKTYPE,'') WHEN '' THEN '' ELSE c.BANKTYPE END AS  BANKTYPE,"
        strFormat = strFormat & " B.DEMANDDAY documentdate, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then '' else C.prepayment end as  prepayment, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then '' else C.fromdate end as  fromdate, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then '' else C.todate end as  todate, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then SUBSTRING(B.TAXYEARMON,5,2) +'월 IFC MALL 광고비' ELSE C.SUMMTEXT END AS summtext, "
        strFormat = strFormat & " case when isnull(A.AMT,0) < 0 then '-' else '+' end AMTGBN, "
        strFormat = strFormat & " B.TAXYEARMON, "
        strFormat = strFormat & " B.TAXNO, "
        strFormat = strFormat & " CASE isnull(B.VOCHNO,'') WHEN '' THEN '' ELSE B.VOCHNO END VOCHNO, "
        strFormat = strFormat & " C.RMSNO, "
        strFormat = strFormat & " C.ERRCODE, "
        strFormat = strFormat & " C.ERRMSG,  "
        strFormat = strFormat & " B.MEDFLAG  "
        strFormat = strFormat & " FROM MD_IFCMALL_AMT A  "
        strFormat = strFormat & " INNER JOIN MD_COMMITAX_HDR B ON A.COMMI_TAX_NO = B.TAXYEARMON + '-' + CAST(TAXNO AS VARCHAR(10))"
        strFormat = strFormat & " LEFT JOIN MD_TRUVOCH_MST C  "
        strFormat = strFormat & " ON A.COMMI_TAX_NO = C.TAXYEARMON + CAST(C.TAXNO AS VARCHAR(10)) And C.GBN = 'S'  "
        strFormat = strFormat & " WHERE B.MEDFLAG IN ('Y')  "
        strFormat = strFormat & " and isnull(B.attr10,0) <> 999999 "
        strFormat = strFormat & " and isnull(B.MERGEFLAG,'') <> '1' "
        strFormat = strFormat & " and isnull(A.EX_AMT,0) <> 0 "
        strFormat = strFormat & " {0} "
      

        strSQL = String.Format(strFormat, strWhere)
        '기본정보 Setting
        With mobjSCGLConfig '기본정보 Config 개체
            Try
                ' DB 접속
                .mobjSCGLSql.SQLConnect(.DBConnStr)
                vntData = .mobjSCGLSql.SQLSelectArr(strSQL, intRowCnt, intColCnt, , True)

                Return vntData
            Catch err As Exception
                ' 오류 전송
                Throw RaiseSysErr(err, CLASS_NAME & ".SelectRtn_SUSUDTL")
            Finally
                ' DB 접속 종료
                .mobjSCGLSql.SQLDisconnect()
            End Try
        End With
    End Function

    Public Function SelectRtn_SUSUDTL_COMMIT(ByVal strInfoXML As String, _
                                             ByRef intRowCnt As Integer, _
                                             ByRef intColCnt As Integer, _
                                             ByVal strTAXYEARMON As String, _
                                             ByVal strTAXNO As String) As Object

        Dim strSQL As String            'SQL문
        Dim strFormat As String         '임시 SQL문
        Dim strWhere As String
        Dim vntData As Object
        Dim Con1, Con2 As String

        SetConfig(strInfoXML)   '기본정보 설정

        Con1 = "" : Con2 = ""

        If strTAXYEARMON <> "" Then Con1 = String.Format(" AND (A.TAXYEARMON = '{0}')", strTAXYEARMON)
        If strTAXNO <> "" Then Con2 = String.Format(" AND (A.TAXNO = '{0}')", strTAXNO)

        strWhere = BuildFields(" ", Con1, Con2)

        strFormat = strFormat & " SELECT  "
        strFormat = strFormat & " '1' GFLAG,  "
        strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then A.DEMANDDAY else b.postingdate end as postingdate,"
        strFormat = strFormat & " replace(A.REAL_MED_BISNO,'-','') customercode, "
        strFormat = strFormat & " REAL_MED_NAME CUSTNAME, "
        strFormat = strFormat & " isnull(b.summ,'')  as  SUMM,  "
        strFormat = strFormat & " isnull(b.ba,'') as BA,"
        strFormat = strFormat & " isnull(b.costcenter,'') as costcenter,"
        strFormat = strFormat & " B.SUMAMT AMT,  "
        strFormat = strFormat & " B.vat VAT,  "
        strFormat = strFormat & " B.SEMU SEMU, "
        strFormat = strFormat & " '8000' BP, "
        strFormat = strFormat & " B.DEMANDDAY DEMANDDAY,  "
        strFormat = strFormat & " B.DUEDATE DUEDATE,  "
        strFormat = strFormat & " replace(A.REAL_MED_BISNO,'-','') VENDOR, "
        strFormat = strFormat & " '' REAL_MED_NAME, "
        strFormat = strFormat & " 'S' gbn, "
        strFormat = strFormat & " isnull(b.ACCOUNT,'') ACCOUNT,  "
        strFormat = strFormat & " isnull(b.DEBTOR,'') DEBTOR,  "
        strFormat = strFormat & " CASE ISNULL(B.BMORDER,'') WHEN '' THEN 'AD0420' ELSE B.BMORDER END AS  BMORDER,"
        strFormat = strFormat & " CASE ISNULL(B.BANKTYPE,'') WHEN '' THEN '' ELSE B.BANKTYPE END AS  BANKTYPE,"
        strFormat = strFormat & " B.documentdate, "
        strFormat = strFormat & " b.prepayment prepayment, "
        strFormat = strFormat & " b.prepayment fromdate, "
        strFormat = strFormat & " b.prepayment todate, "
        strFormat = strFormat & " B.SUMMTEXT summtext, "
        strFormat = strFormat & " case when isnull(A.AMT,0) < 0 then '-' else '+' end AMTGBN, "
        strFormat = strFormat & " B.TAXYEARMON, "
        strFormat = strFormat & " B.TAXNO, "
        strFormat = strFormat & " B.vochno vochno, "
        strFormat = strFormat & " B.RMSNO, "
        strFormat = strFormat & " B.ERRCODE, "
        strFormat = strFormat & " B.ERRMSG,  "
        strFormat = strFormat & " A.MEDFLAG  "
        strFormat = strFormat & " FROM MD_COMMITAX_HDR  A  "
        strFormat = strFormat & " LEFT JOIN MD_TRUVOCH_MST B  "
        strFormat = strFormat & " ON A.TAXYEARMON = B.TAXYEARMON AND A.TAXNO = B.TAXNO And B.GBN = 'S'  "
        strFormat = strFormat & " WHERE A.MEDFLAG IN ('Y')  "
        strFormat = strFormat & " and isnull(A.attr10,0) <> 999999 "
        strFormat = strFormat & " and isnull(A.MERGEFLAG,'') <> '1'  "
        strFormat = strFormat & " {0} "

        strSQL = String.Format(strFormat, strWhere)
        '기본정보 Setting
        With mobjSCGLConfig '기본정보 Config 개체
            Try
                ' DB 접속
                .mobjSCGLSql.SQLConnect(.DBConnStr)
                vntData = .mobjSCGLSql.SQLSelectArr(strSQL, intRowCnt, intColCnt, , True)

                Return vntData
            Catch err As Exception
                ' 오류 전송
                Throw RaiseSysErr(err, CLASS_NAME & ".SelectRtn_HDR")
            Finally
                ' DB 접속 종료
                .mobjSCGLSql.SQLDisconnect()
            End Try
        End With
    End Function

    Public Function SelectRtn_OUT(ByVal strInfoXML As String, _
                                  ByRef intRowCnt As Integer, _
                                  ByRef intColCnt As Integer, _
                                  ByVal strYEARMON As String, _
                                  ByVal strCLIENTCODE As String, _
                                  ByVal strCLIENTNAME As String, _
                                  ByVal strGBN As String) As Object

        Dim strSQL, strFormat, strWhere As String
        Dim vntData As Object
        Dim Con1, Con2, Con3, Con4, Con5, Con6, Con7, Con8 As String

        Con1 = "" : Con2 = "" : Con3 = "" : Con4 = "" : Con5 = "" : Con6 = "" : Con7 = "" : Con8 = ""

        SetConfig(strInfoXML)   '기본정보 설정
        With mobjSCGLConfig

            If strYEARMON <> "" Then Con1 = String.Format(" AND (A.YEARMON = '{0}')", strYEARMON)
            If strCLIENTCODE <> "" Then Con2 = String.Format(" AND A.DEPT_CD = '{0}'", strCLIENTCODE)
            If strCLIENTNAME <> "" Then Con3 = String.Format(" AND DBO.SC_GET_HIGHCUSTNAME_FUN(A.DEPT_CD)  LIKE '%{0}%'", strCLIENTNAME)

            'TRU_VOCH_NO 이 다름
            If strGBN <> "" Then
                If strGBN = "rdT" Then
                    Con6 = " AND ISNULL(A.EX_VOCH_NO,'') <> '' "
                ElseIf strGBN = "rdF" Then
                    Con6 = " AND ISNULL(A.EX_VOCH_NO,'') = '' AND ISNULL(B.ERRMSG,'') = '' "
                ElseIf strGBN = "rdE" Then
                    Con6 = " AND ISNULL(B.ERRMSG,'') <> '' "
                ElseIf strGBN = "rdA" Then
                    Con6 = " "
                End If
            End If

            '조회 필드 설정
            strWhere = BuildFields(" ", Con1, Con2, Con3, Con4, Con5, Con6, Con7, Con8)

            strFormat = strFormat & " SELECT "
            strFormat = strFormat & " 0 chk, "
            strFormat = strFormat & " '1' GFLAG, "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then replace(CONVERT(VARCHAR(10), dateadd(d,-day(dateadd(m,1,a.demandday)), dateadd(m,1, a.demandday  )),121 ) ,'-','') else b.postingdate end as postingdate,"
            strFormat = strFormat & " REPLACE(DBO.SC_GET_BUSINO_FUN(A.EXCLIENTCODE),'-','') CUSTOMERCODE, "
            strFormat = strFormat & " DBO.SC_GET_HIGHCUSTNAME_FUN(A.EXCLIENTCODE) CUSTNAME, "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then RTRIM(LTRIM(CONVERT(CHAR(17),RTRIM(LTRIM(DBO.SC_GET_HIGHCUSTNAME_FUN(A.EXCLIENTCODE) )))))+'외주비' ELSE B.SUMM END AS SUMM,  "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then dbo.SC_GET_BA(A.DEPT_CD) else b.ba end as BA,"
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then dbo.SC_GET_CCTR(A.DEPT_CD) else b.costcenter end as costcenter,"
            strFormat = strFormat & " case when isnull(A.EX_AMT,0) < 0 then abs(isnull(A.EX_AMT,0)) else  dbo.lpad(A.EX_AMT,13,'0') end  amt, "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then case when isnull(A.EX_AMT,0) < 0 then abs(FLOOR(A.EX_AMT* 0.1)) else dbo.lpad(FLOOR(A.EX_AMT* 0.1),13,'0') end else isnull(b.vat,0) end vat,  "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then 'I0' ELSE ISNULL(SEMU,'') END AS SEMU, "
            strFormat = strFormat & " '8000' BP,"
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then CONVERT(CHAR(8),DATEADD(DD,90,replace(CONVERT(VARCHAR(10), dateadd(d,-day(dateadd(m,1,a.demandday)), dateadd(m,1, a.demandday  )),121 ) ,'-','') ),112) ELSE B.DEMANDDAY END AS DEMANDDAY, "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then CONVERT(CHAR(8),DATEADD(DD,60,A.DEMANDDAY ),112) ELSE B.DUEDATE END AS DUEDATE, "
            strFormat = strFormat & " REPLACE(DBO.SC_GET_BUSINO_FUN(A.EXCLIENTCODE),'-','') VENDOR, "
            strFormat = strFormat & " DBO.SC_GET_HIGHCUSTNAME_FUN(A.EXCLIENTCODE) VENDORNAME, "
            strFormat = strFormat & " 'O' GBN,  "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then '681180' else b.account end as  ACCOUNT, "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then '404103' else b.DEBTOR end as  DEBTOR, "
            strFormat = strFormat & " CASE ISNULL(B.BMORDER,'') WHEN '' THEN 'AD0420' ELSE B.BMORDER END AS  BMORDER,"
            strFormat = strFormat & " CASE ISNULL(B.BANKTYPE,'') WHEN '' THEN '' ELSE B.BANKTYPE END AS  BANKTYPE,"
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then A.DEMANDDAY else b.documentdate end as documentdate,"
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then '' else b.prepayment end as  prepayment,"
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then '' else b.fromdate end as  fromdate,"
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then '' else b.todate end as  todate,"
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then DBO.SC_GET_HIGHCUSTNAME_FUN(EXCLIENTCODE)+'- 외주용역비' ELSE B.SUMMTEXT END AS SUMMTEXT ,  "
            strFormat = strFormat & " case when isnull(A.AMT,0) < 0 then '-' else '+' end AMTGBN,"
            strFormat = strFormat & " YEARMON TAXYEARMON,  "
            strFormat = strFormat & " A.SEQ TAXNO,  "
            strFormat = strFormat & " case EX_VOCH_NO when '' then '' else EX_VOCH_NO end VOCHNO, "
            strFormat = strFormat & " ISNULL(B.RMSNO,'') RMSNO, "
            strFormat = strFormat & " ISNULL(B.ERRCODE,'') ERRCODE, "
            strFormat = strFormat & " ISNULL(B.ERRMSG,'') ERRMSG,  "
            strFormat = strFormat & " ISNULL(B.PAYCODE,'') PAYCODE,"
            strFormat = strFormat & " 'YO' MEDFLAG, "
            strFormat = strFormat & " DENSE_RANK() OVER(ORDER BY A.EXCLIENTCODE DESC) TRANSRANK "
            strFormat = strFormat & " FROM MD_IFCMALL_AMT A LEFT JOIN MD_TRUVOCH_MST B   "
            strFormat = strFormat & " ON A.YEARMON = B.TAXYEARMON AND A.SEQ = B.TAXNO And B.GBN = 'O' and b.medflag = 'YO'"
            strFormat = strFormat & " WHERE 1=1 and isnull(commi_tax_no,'') <> '' and  isnull(amt,0) <> 0 "
            strFormat = strFormat & " {0} "
            strFormat = strFormat & " ORDER by DBO.SC_GET_HIGHCUSTNAME_FUN(A.EXCLIENTCODE) "

            strSQL = String.Format(strFormat, strWhere)

            '데이터 조회
            Try
                .mobjSCGLSql.SQLConnect(.DBConnStr)
                vntData = .mobjSCGLSql.SQLSelectArr(strSQL, intRowCnt, intColCnt, , True)
                Return vntData
            Catch err As Exception
                Throw RaiseSysErr(err, CLASS_NAME & ".SelectRtn_OUT")
            Finally
                .mobjSCGLSql.SQLDisconnect()
            End Try
        End With
    End Function

    Public Function SelectRtn_EOM(ByVal strInfoXML As String, _
                                  ByRef intRowCnt As Integer, _
                                  ByRef intColCnt As Integer, _
                                  ByVal strYEARMON As String, _
                                  ByVal strCLIENTCODE As String, _
                                  ByVal strCLIENTNAME As String, _
                                  ByVal strGBN As String) As Object

        Dim strSQL, strFormat, strWhere As String
        Dim vntData As Object
        Dim Con1, Con2, Con3, Con4, Con5, Con6, Con7, Con8 As String

        Con1 = "" : Con2 = "" : Con3 = "" : Con4 = "" : Con5 = "" : Con6 = "" : Con7 = "" : Con8 = ""

        SetConfig(strInfoXML)   '기본정보 설정
        With mobjSCGLConfig

            If strYEARMON <> "" Then Con1 = String.Format(" AND (A.YEARMON = '{0}')", strYEARMON)
            If strCLIENTCODE <> "" Then Con2 = String.Format(" AND A.EXCLIENTCODE = '{0}'", strCLIENTCODE)
            If strCLIENTNAME <> "" Then Con3 = String.Format(" AND DBO.SC_GET_HIGHCUSTNAME_FUN(A.EXCLIENTCODE)  LIKE '%{0}%'", strCLIENTNAME)

            'EX_VOCH_NO 이 다름
            If strGBN <> "" Then
                If strGBN = "rdT" Then
                    Con6 = " AND ISNULL(A.VOCHNO,'') <> '' "
                ElseIf strGBN = "rdF" Then
                    Con6 = " AND ISNULL(A.VOCHNO,'') = '' AND ISNULL(B.ERRMSG,'') = '' "
                ElseIf strGBN = "rdE" Then
                    Con6 = " AND ISNULL(B.ERRMSG,'') <> '' "
                ElseIf strGBN = "rdA" Then
                    Con6 = " "
                End If
            End If

            '조회 필드 설정
            strWhere = BuildFields(" ", Con1, Con2, Con3, Con4, Con5, Con6, Con7, Con8)

            strFormat = strFormat & " SELECT "
            strFormat = strFormat & " 0 chk, "
            strFormat = strFormat & " '1' GFLAG, "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then replace(CONVERT(VARCHAR(10), dateadd(d,-day(dateadd(m,1,a.demandday)), dateadd(m,1, a.demandday  )),121 ) ,'-','') else b.postingdate end as postingdate,"
            strFormat = strFormat & " REPLACE(DBO.SC_GET_BUSINO_FUN(A.EXCLIENTCODE),'-','') CUSTOMERCODE, "
            strFormat = strFormat & " DBO.SC_GET_HIGHCUSTNAME_FUN(A.EXCLIENTCODE) CUSTNAME, "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then RTRIM(LTRIM(CONVERT(CHAR(17),RTRIM(LTRIM(DBO.SC_GET_HIGHCUSTNAME_FUN(A.EXCLIENTCODE))))))+'외주비' ELSE B.SUMM END AS SUMM,  "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then dbo.SC_GET_BA(A.DEPT_CD) else b.ba end as BA,"
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then dbo.SC_GET_CCTR(A.DEPT_CD) else b.costcenter end as costcenter,"
            strFormat = strFormat & " case when isnull(A.AMT,0) < 0 then abs(isnull(A.AMT,0)) else  dbo.lpad(A.AMT,13,'0') end  amt, "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then case when isnull(A.AMT,0) < 0 then abs(FLOOR(A.AMT* 0.1)) else dbo.lpad(FLOOR(A.AMT* 0.1),13,'0') end else isnull(b.vat,0) end vat,  "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then 'I0' ELSE ISNULL(SEMU,'') END AS SEMU, "
            strFormat = strFormat & " '8000' BP,"
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then CONVERT(CHAR(8),DATEADD(DD,90,replace(CONVERT(VARCHAR(10), dateadd(d,-day(dateadd(m,1,a.demandday)), dateadd(m,1, a.demandday  )),121 ) ,'-','') ),112) ELSE B.DEMANDDAY END AS DEMANDDAY, "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then CONVERT(CHAR(8),DATEADD(DD,60,A.DEMANDDAY ),112) ELSE B.DUEDATE END AS DUEDATE, "
            strFormat = strFormat & " REPLACE(DBO.SC_GET_BUSINO_FUN(A.EXCLIENTCODE),'-','') VENDOR, "
            strFormat = strFormat & " DBO.SC_GET_HIGHCUSTNAME_FUN(A.EXCLIENTCODE) VENDORNAME, "
            strFormat = strFormat & " 'O' GBN,  "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then '681180' else b.account end as  ACCOUNT, "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then '404103' else b.DEBTOR end as  DEBTOR, "
            strFormat = strFormat & " CASE ISNULL(B.BMORDER,'') WHEN '' THEN 'AD0420' ELSE B.BMORDER END AS  BMORDER,"
            strFormat = strFormat & " CASE ISNULL(B.BANKTYPE,'') WHEN '' THEN '' ELSE B.BANKTYPE END AS  BANKTYPE,"
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then A.DEMANDDAY else b.documentdate end as documentdate,"
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then '' else b.prepayment end as  prepayment,"
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then '' else b.fromdate end as  fromdate,"
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then '' else b.todate end as  todate,"
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then DBO.SC_GET_HIGHCUSTNAME_FUN(A.EXCLIENTCODE)+'- 대행수수료분배금' ELSE B.SUMMTEXT END AS SUMMTEXT ,  "
            strFormat = strFormat & " case when isnull(A.AMT,0) < 0 then '-' else '+' end AMTGBN,"
            strFormat = strFormat & " YEARMON TAXYEARMON,  "
            strFormat = strFormat & " A.SEQ TAXNO,  "
            strFormat = strFormat & " case  cast(case a.vochno when '' then '' else a.vochno end as varchar) when '' then null  "
            strFormat = strFormat & " else cast(case a.vochno when '' then '' else a.vochno end as varchar)  end  vochno,"
            strFormat = strFormat & " ISNULL(B.RMSNO,'') RMSNO, "
            strFormat = strFormat & " ISNULL(B.ERRCODE,'') ERRCODE, "
            strFormat = strFormat & " ISNULL(B.ERRMSG,'') ERRMSG,  "
            strFormat = strFormat & " ISNULL(B.PAYCODE,'') PAYCODE,"
            strFormat = strFormat & " 'YO2' MEDFLAG, "
            strFormat = strFormat & " REPLACE(DBO.SC_GET_BUSINO_FUN(A.EXCLIENTCODE),'-','') REAL_MED_BUSINO, "
            strFormat = strFormat & " DBO.SC_GET_HIGHCUSTNAME_FUN(A.EXCLIENTCODE) REAL_MED_NAME, "
            strFormat = strFormat & " DENSE_RANK() OVER(ORDER BY A.EXCLIENTCODE DESC) TRANSRANK "
            strFormat = strFormat & " FROM MD_IFCMALL_OUT A LEFT JOIN MD_TRUVOCH_MST B   "
            strFormat = strFormat & " ON A.YEARMON = B.TAXYEARMON AND A.SEQ = B.TAXNO And B.GBN = 'O' and b.medflag = 'YO2'"
            strFormat = strFormat & " WHERE 1=1 and isnull(COMMI_TRANS_NO,'') <> '' and  isnull(amt,0) <> 0 "
            strFormat = strFormat & " {0} "
            strFormat = strFormat & " and len(A.EXCLIENTCODE) = '6' "
            strFormat = strFormat & " AND ISNULL(A.GUBUN,0) = 0 "
            strFormat = strFormat & " order by DBO.SC_GET_HIGHCUSTNAME_FUN(A.EXCLIENTCODE)"


            strSQL = String.Format(strFormat, strWhere)

            '데이터 조회
            Try
                .mobjSCGLSql.SQLConnect(.DBConnStr)
                vntData = .mobjSCGLSql.SQLSelectArr(strSQL, intRowCnt, intColCnt, , True)
                Return vntData
            Catch err As Exception
                Throw RaiseSysErr(err, CLASS_NAME & ".SelectRtn_EOM")
            Finally
                .mobjSCGLSql.SQLDisconnect()
            End Try
        End With
    End Function


    Public Function SelectRtn_EOMSUSU(ByVal strInfoXML As String, _
                                      ByRef intRowCnt As Integer, _
                                      ByRef intColCnt As Integer, _
                                      ByVal strYEARMON As String, _
                                      ByVal strCLIENTCODE As String, _
                                      ByVal strCLIENTNAME As String, _
                                      ByVal strGBN As String) As Object

        Dim strSQL, strFormat, strWhere As String
        Dim vntData As Object
        Dim Con1, Con2, Con3, Con4, Con5, Con6, Con7, Con8 As String

        Con1 = "" : Con2 = "" : Con3 = "" : Con4 = "" : Con5 = "" : Con6 = "" : Con7 = "" : Con8 = ""

        SetConfig(strInfoXML)   '기본정보 설정
        With mobjSCGLConfig

            If strYEARMON <> "" Then Con1 = String.Format(" AND (A.TAXYEARMON = '{0}')", strYEARMON)
            If strCLIENTCODE <> "" Then Con2 = String.Format(" AND A.CLIENTCODE = '{0}'", strCLIENTCODE)
            If strCLIENTNAME <> "" Then Con3 = String.Format(" AND DBO.SC_GET_HIGHCUSTNAME_FUN(A.CLIENTCODE)  LIKE '%{0}%'", strCLIENTNAME)

            If strGBN <> "" Then
                If strGBN = "rdT" Then
                    Con6 = " AND ISNULL(A.VOCHNO,'') <> '' "
                ElseIf strGBN = "rdF" Then
                    Con6 = " AND ISNULL(A.VOCHNO,'') = '' AND ISNULL(B.ERRMSG,'') = '' "
                ElseIf strGBN = "rdE" Then
                    Con6 = " AND ISNULL(B.ERRMSG,'') <> '' "
                ElseIf strGBN = "rdA" Then
                    Con6 = " "
                End If
            End If

            '조회 필드 설정
            strWhere = BuildFields(" ", Con1, Con2, Con3, Con4, Con5, Con6, Con7, Con8)

            strFormat = strFormat & " SELECT "
            strFormat = strFormat & " 0 CHK, "
            strFormat = strFormat & " '1' GFLAG, "
            strFormat = strFormat & " CASE ISNULL(B.POSTINGDATE,'') WHEN '' THEN A.DEMANDDAY ELSE B.POSTINGDATE END AS POSTINGDATE,"
            strFormat = strFormat & " REPLACE(A.REAL_MED_BISNO,'-','') CUSTOMERCODE,"
            strFormat = strFormat & " REAL_MED_NAME CUSTNAME,"
            strFormat = strFormat & " CASE ISNULL(B.SUMM,'') WHEN '' THEN SUBSTRING(A.TAXYEARMON,5,2) +'월IFC MALL 광고비'  ELSE B.SUMM END AS  SUMM, "
            strFormat = strFormat & " CASE WHEN ISNULL(A.AMT,0) < 0 THEN ABS(ISNULL(A.AMT,0)) ELSE  DBO.LPAD(A.AMT,13,'0') END  AMT, "
            strFormat = strFormat & " CASE ISNULL(B.VAT,0) WHEN 0 THEN CASE WHEN ISNULL(A.VAT,0) < 0 THEN ABS(ISNULL(A.VAT,0)) ELSE DBO.LPAD(A.VAT,13,'0') END ELSE ISNULL(B.VAT,0) END VAT, "
            strFormat = strFormat & " CASE ISNULL(B.SEMU,'') WHEN '' THEN 'B0' ELSE B.SEMU END AS SEMU,"
            strFormat = strFormat & " CASE ISNULL(B.BP,'') WHEN '' THEN '8000' ELSE B.BP END AS BP,"
            strFormat = strFormat & " CASE ISNULL(B.DEMANDDAY,'') WHEN '' THEN CONVERT(CHAR(8),DATEADD(DD,60,REPLACE(CONVERT(VARCHAR(10), DATEADD(D,-DAY(DATEADD(M,1,A.DEMANDDAY)), DATEADD(M,1, A.DEMANDDAY  )),121 ) ,'-','') ),112) ELSE B.DEMANDDAY END AS DEMANDDAY, "
            strFormat = strFormat & " CASE ISNULL(B.DUEDATE,'') WHEN '' THEN CONVERT(CHAR(8),DATEADD(DD,60,A.DEMANDDAY ),112) ELSE B.DUEDATE END AS DUEDATE, "
            strFormat = strFormat & " REPLACE(A.REAL_MED_BISNO,'-','') VENDOR,"
            strFormat = strFormat & " '' REAL_MED_NAME,"
            strFormat = strFormat & " 'YO3' GBN,"
            strFormat = strFormat & " A.DEMANDDAY DOCUMENTDATE,"
            strFormat = strFormat & " CASE ISNULL(B.PREPAYMENT,'') WHEN '' THEN '' ELSE B.PREPAYMENT END AS  PREPAYMENT,"
            strFormat = strFormat & " CASE ISNULL(B.PREPAYMENT,'') WHEN '' THEN '' ELSE B.FROMDATE END AS  FROMDATE,"
            strFormat = strFormat & " CASE ISNULL(B.PREPAYMENT,'') WHEN '' THEN '' ELSE B.TODATE END AS  TODATE,"
            strFormat = strFormat & " CASE ISNULL(B.SUMMTEXT,'') WHEN '' THEN SUBSTRING(A.TAXYEARMON,5,2) +'월IFC MALL 광고비' ELSE B.SUMMTEXT END AS SUMMTEXT,"
            strFormat = strFormat & " CASE WHEN ISNULL(A.AMT,0) < 0 THEN '-' ELSE '+' END AMTGBN,"
            strFormat = strFormat & " A.TAXYEARMON,"
            strFormat = strFormat & " A.TAXNO,"
            strFormat = strFormat & " CASE ISNULL(A.VOCHNO,'') WHEN '' THEN '' ELSE A.VOCHNO END VOCHNO,"
            strFormat = strFormat & " B.RMSNO,"
            strFormat = strFormat & " B.ERRCODE,"
            strFormat = strFormat & " B.ERRMSG, "
            strFormat = strFormat & " 'YO3' MEDFLAG "
            strFormat = strFormat & " FROM MD_COMMITAX_HDR  A "
            strFormat = strFormat & " LEFT JOIN MD_TRUVOCH_MST B "
            strFormat = strFormat & " ON A.TAXYEARMON = B.TAXYEARMON AND A.TAXNO = B.TAXNO AND B.GBN = 'YO3' AND B.MEDFLAG = 'YO3' "
            strFormat = strFormat & " WHERE A.MEDFLAG IN ('Y') "
            strFormat = strFormat & " AND ISNULL(A.ATTR10,0) <> 999999"
            strFormat = strFormat & " AND ISNULL(A.MERGEFLAG,'') <> '1'  AND ISNULL(B.DEBTOR,'') = '' "
            strFormat = strFormat & " AND ISNULL(A.ATTR01,0) = 1 "
            strFormat = strFormat & " {0} "

            strSQL = String.Format(strFormat, strWhere)

            '데이터 조회
            Try
                .mobjSCGLSql.SQLConnect(.DBConnStr)
                vntData = .mobjSCGLSql.SQLSelectArr(strSQL, intRowCnt, intColCnt, , True)
                Return vntData
            Catch err As Exception
                Throw RaiseSysErr(err, CLASS_NAME & ".SelectRtn_EOMSUSU")
            Finally
                .mobjSCGLSql.SQLDisconnect()
            End Try
        End With
    End Function


    Public Function SelectRtn_EOMSUSUDTL(ByVal strInfoXML As String, _
                                         ByRef intRowCnt As Integer, _
                                         ByRef intColCnt As Integer, _
                                         ByVal strTAXYEARMON As String, _
                                         ByVal strTAXNO As String, _
                                         ByVal strPREPAYMENT As String) As Object

        Dim strSQL As String            'SQL문 
        Dim strFormat As String         '임시 SQL문
        Dim strWhere As String
        Dim vntData As Object
        Dim Con1, Con2 As String

        SetConfig(strInfoXML)   '기본정보 설정

        Con1 = "" : Con2 = ""

        If strTAXYEARMON <> "" Then Con1 = String.Format(" AND (B.TAXYEARMON = '{0}')", strTAXYEARMON)
        If strTAXNO <> "" Then Con2 = String.Format(" AND (B.TAXNO = '{0}')", strTAXNO)

        strWhere = BuildFields(" ", Con1, Con2)

        strFormat = strFormat & " SELECT  "
        strFormat = strFormat & " '1' SORT,"
        strFormat = strFormat & " '1' GFLAG,  "
        strFormat = strFormat & " CASE ISNULL(C.POSTINGDATE,'') WHEN '' THEN B.DEMANDDAY ELSE C.POSTINGDATE END AS POSTINGDATE,"
        strFormat = strFormat & " REPLACE(B.REAL_MED_BISNO,'-','') CUSTOMERCODE, "
        strFormat = strFormat & " REAL_MED_NAME CUSTNAME, "
        strFormat = strFormat & " CASE ISNULL(C.POSTINGDATE,'') WHEN '' THEN SUBSTRING(B.TAXYEARMON,5,2) +'월 IFC MALL 광고비'  ELSE C.SUMM END AS  SUMM,  "
        strFormat = strFormat & " CASE ISNULL(C.POSTINGDATE,'') WHEN '' THEN DBO.SC_GET_BA(B.DEPT_CD) ELSE C.BA END AS BA,"
        strFormat = strFormat & " CASE ISNULL(C.POSTINGDATE,'') WHEN '' THEN DBO.SC_GET_CCTR(B.DEPT_CD) ELSE C.COSTCENTER END AS COSTCENTER,"
        strFormat = strFormat & " CASE ISNULL(C.POSTINGDATE,'') WHEN '' THEN CASE WHEN ISNULL(B.AMT,0) < 0 THEN ABS(ISNULL(B.AMT,0)) ELSE DBO.LPAD(B.AMT,13,'0') END ELSE ISNULL(C.SUMAMT,0) END AMT,  "
        strFormat = strFormat & " CASE ISNULL(C.POSTINGDATE,'') WHEN '' THEN CASE WHEN ISNULL(B.VAT,0) < 0 THEN ABS(ISNULL(B.VAT,0)) ELSE DBO.LPAD(B.VAT,13,'0') END ELSE ISNULL(C.VAT,0) END VAT,  "
        strFormat = strFormat & " CASE ISNULL(C.POSTINGDATE,'') WHEN '' THEN 'B0' ELSE ISNULL(SEMU,'') END AS SEMU, "
        strFormat = strFormat & " '8000' BP, "
        strFormat = strFormat & " CASE ISNULL(C.POSTINGDATE,'') WHEN '' THEN CONVERT(CHAR(8),DATEADD(DD,60,REPLACE(CONVERT(VARCHAR(10), DATEADD(D,-DAY(DATEADD(M,1,B.DEMANDDAY)), DATEADD(M,1, B.DEMANDDAY  )),121 ) ,'-','') ),112) ELSE C.DEMANDDAY END AS DEMANDDAY,  "
        strFormat = strFormat & " CASE ISNULL(C.POSTINGDATE,'') WHEN '' THEN CONVERT(CHAR(8),DATEADD(DD,60,B.DEMANDDAY ),112) ELSE C.DUEDATE END AS DUEDATE,  "
        strFormat = strFormat & " REPLACE(B.REAL_MED_BISNO,'-','') VENDOR, "
        strFormat = strFormat & " '' REAL_MED_NAME, "
        strFormat = strFormat & " 'YO3' GBN, "
        strFormat = strFormat & " CASE ISNULL(C.POSTINGDATE,'') WHEN '' THEN '200400' ELSE C.ACCOUNT END AS  ACCOUNT,  "
        strFormat = strFormat & " ''  DEBTOR,  "
        strFormat = strFormat & " CASE ISNULL(C.BMORDER,'') WHEN '' THEN 'AD0420' ELSE C.BMORDER END AS  BMORDER,"
        strFormat = strFormat & " CASE ISNULL(C.BANKTYPE,'') WHEN '' THEN '' ELSE C.BANKTYPE END AS  BANKTYPE,"
        strFormat = strFormat & " B.DEMANDDAY DOCUMENTDATE, "
        strFormat = strFormat & " CASE ISNULL(C.POSTINGDATE,'') WHEN '' THEN '' ELSE C.PREPAYMENT END AS  PREPAYMENT, "
        strFormat = strFormat & " CASE ISNULL(C.POSTINGDATE,'') WHEN '' THEN '' ELSE C.FROMDATE END AS  FROMDATE, "
        strFormat = strFormat & " CASE ISNULL(C.POSTINGDATE,'') WHEN '' THEN '' ELSE C.TODATE END AS  TODATE, "
        strFormat = strFormat & " CASE ISNULL(C.POSTINGDATE,'') WHEN '' THEN SUBSTRING(B.TAXYEARMON,5,2) +'월 IFC MALL 광고비' ELSE C.SUMMTEXT END AS SUMMTEXT, "
        strFormat = strFormat & " CASE WHEN ISNULL(B.AMT,0) < 0 THEN '-' ELSE '+' END AMTGBN, "
        strFormat = strFormat & " B.TAXYEARMON, "
        strFormat = strFormat & " B.TAXNO, "
        strFormat = strFormat & " CASE ISNULL(B.VOCHNO,'') WHEN '' THEN '' ELSE B.VOCHNO END VOCHNO, "
        strFormat = strFormat & " C.RMSNO, "
        strFormat = strFormat & " C.ERRCODE, "
        strFormat = strFormat & " C.ERRMSG,  "
        strFormat = strFormat & " 'YO3' MEDFLAG  "
        strFormat = strFormat & " FROM MD_COMMITAX_HDR B "
        strFormat = strFormat & " LEFT JOIN MD_TRUVOCH_MST C  "
        strFormat = strFormat & " ON B.TAXYEARMON = C.TAXYEARMON AND B.TAXNO = C.TAXNO AND C.GBN = 'YO3' AND C.MEDFLAG = 'YO3' "
        strFormat = strFormat & " WHERE B.MEDFLAG IN ('Y')  "
        strFormat = strFormat & " AND ISNULL(B.ATTR10,0) <> 999999 "
        strFormat = strFormat & " AND ISNULL(B.MERGEFLAG,'') <> '1'  AND ISNULL(C.DEBTOR,'') = '' "
        strFormat = strFormat & " AND ISNULL(B.ATTR01,0) = 1"
        strFormat = strFormat & " {0} "

        strFormat = strFormat & " UNION ALL"

        strFormat = strFormat & " SELECT  "
        strFormat = strFormat & " '2' SORT,"
        strFormat = strFormat & " '1' GFLAG,  "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then B.DEMANDDAY else C.postingdate end as postingdate,"
        strFormat = strFormat & " replace(B.REAL_MED_BISNO,'-','') customercode, "
        strFormat = strFormat & " B.REAL_MED_NAME CUSTNAME, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then SUBSTRING(B.TAXYEARMON,5,2) +'월 IFC MALL 광고비'  else b.summ end as  SUMM,  "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then dbo.SC_GET_BA(A.DEPT_CD) else C.ba end as BA,"
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then dbo.SC_GET_CCTR(A.DEPT_CD) else C.costcenter end as costcenter,"
        strFormat = strFormat & " case when isnull(A.AMT,0) < 0 then abs(isnull(A.AMT,0)) else  dbo.lpad(A.AMT,13,'0') end  amt,  "
        strFormat = strFormat & " 0 vat,  "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then 'B0' ELSE ISNULL(C.SEMU,'') END AS SEMU, "
        strFormat = strFormat & " '8000' BP, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then CONVERT(CHAR(8),DATEADD(DD,60,replace(CONVERT(VARCHAR(10), dateadd(d,-day(dateadd(m,1,B.demandday)), dateadd(m,1, B.demandday  )),121 ) ,'-','') ),112) ELSE C.DEMANDDAY END AS DEMANDDAY,  "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then CONVERT(CHAR(8),DATEADD(DD,60,B.DEMANDDAY ),112) ELSE C.DUEDATE END AS DUEDATE,  "
        strFormat = strFormat & " replace(B.REAL_MED_BISNO,'-','') VENDOR, "
        strFormat = strFormat & " '' REAL_MED_NAME, "
        strFormat = strFormat & " 'YO3' gbn, "
        strFormat = strFormat & " ''  ACCOUNT,  "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then '558830' else C.DEBTOR end as  DEBTOR,  "
        strFormat = strFormat & " CASE ISNULL(c.BMORDER,'') WHEN '' THEN 'AD0420' ELSE c.BMORDER END AS  BMORDER,"
        strFormat = strFormat & " CASE ISNULL(c.BANKTYPE,'') WHEN '' THEN '' ELSE c.BANKTYPE END AS  BANKTYPE,"
        strFormat = strFormat & " B.DEMANDDAY documentdate, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then '' else C.prepayment end as  prepayment, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then '' else C.fromdate end as  fromdate, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then '' else C.todate end as  todate, "
        strFormat = strFormat & " CASE isnull(C.postingdate,'') when '' then SUBSTRING(B.TAXYEARMON,5,2) +'월 IFC MALL 광고비' ELSE C.SUMMTEXT END AS summtext, "
        strFormat = strFormat & " case when isnull(A.AMT,0) < 0 then '-' else '+' end AMTGBN, "
        strFormat = strFormat & " B.TAXYEARMON, "
        strFormat = strFormat & " B.TAXNO, "
        strFormat = strFormat & " CASE ISNULL(B.VOCHNO,'') WHEN '' THEN '' ELSE B.VOCHNO END VOCHNO, "
        strFormat = strFormat & " C.RMSNO, "
        strFormat = strFormat & " C.ERRCODE, "
        strFormat = strFormat & " C.ERRMSG,  "
        strFormat = strFormat & " 'YO3' MEDFLAG  "
        strFormat = strFormat & " FROM MD_IFCMALL_OUT A  "
        strFormat = strFormat & " INNER JOIN MD_COMMITAX_HDR B ON A.COMMI_TAX_NO = B.TAXYEARMON + '-' + CAST(TAXNO AS VARCHAR(10))"
        strFormat = strFormat & " LEFT JOIN MD_TRUVOCH_MST C  "
        strFormat = strFormat & " ON B.TAXYEARMON = C.TAXYEARMON AND B.TAXNO = C.TAXNO AND C.GBN = 'YO3' AND C.MEDFLAG = 'YO3' "
        strFormat = strFormat & " WHERE B.MEDFLAG IN ('Y')  "
        strFormat = strFormat & " and isnull(B.attr10,0) <> 999999 "
        strFormat = strFormat & " and isnull(B.MERGEFLAG,'') <> '1' "
        strFormat = strFormat & " AND ISNULL(B.ATTR01,0) = 1"
        strFormat = strFormat & " {0} "

        strSQL = String.Format(strFormat, strWhere)
        '기본정보 Setting
        With mobjSCGLConfig '기본정보 Config 개체
            Try
                ' DB 접속
                .mobjSCGLSql.SQLConnect(.DBConnStr)
                vntData = .mobjSCGLSql.SQLSelectArr(strSQL, intRowCnt, intColCnt, , True)

                Return vntData
            Catch err As Exception
                ' 오류 전송
                Throw RaiseSysErr(err, CLASS_NAME & ".SelectRtn_SUSUDTL")
            Finally
                ' DB 접속 종료
                .mobjSCGLSql.SQLDisconnect()
            End Try
        End With
    End Function

    Public Function SelectRtn_OUTSUSU(ByVal strInfoXML As String, _
                                      ByRef intRowCnt As Integer, _
                                      ByRef intColCnt As Integer, _
                                      ByVal strYEARMON As String, _
                                      ByVal strCLIENTCODE As String, _
                                      ByVal strCLIENTNAME As String, _
                                      ByVal strGBN As String) As Object

        Dim strSQL, strFormat, strWhere As String
        Dim vntData As Object
        Dim Con1, Con2, Con3, Con4, Con5, Con6, Con7, Con8 As String

        Con1 = "" : Con2 = "" : Con3 = "" : Con4 = "" : Con5 = "" : Con6 = "" : Con7 = "" : Con8 = ""

        SetConfig(strInfoXML)   '기본정보 설정
        With mobjSCGLConfig

            If strYEARMON <> "" Then Con1 = String.Format(" AND (A.YEARMON = '{0}')", strYEARMON)
            If strCLIENTCODE <> "" Then Con2 = String.Format(" AND A.REAL_MED_CODE = '{0}'", strCLIENTCODE)
            If strCLIENTNAME <> "" Then Con3 = String.Format(" AND DBO.SC_GET_HIGHCUSTNAME_FUN(A.REAL_MED_CODE)  LIKE '%{0}%'", strCLIENTNAME)

            'EX_VOCH_NO 이 다름
            If strGBN <> "" Then
                If strGBN = "rdT" Then
                    Con6 = " AND ISNULL(A.VOCHNO,'') <> '' "
                ElseIf strGBN = "rdF" Then
                    Con6 = " AND ISNULL(A.VOCHNO,'') = '' AND ISNULL(B.ERRMSG,'') = '' "
                ElseIf strGBN = "rdE" Then
                    Con6 = " AND ISNULL(B.ERRMSG,'') <> '' "
                ElseIf strGBN = "rdA" Then
                    Con6 = " "
                End If
            End If

            '조회 필드 설정
            strWhere = BuildFields(" ", Con1, Con2, Con3, Con4, Con5, Con6, Con7, Con8)

            strFormat = strFormat & " SELECT "
            strFormat = strFormat & " 0 chk, "
            strFormat = strFormat & " '1' GFLAG, "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then replace(CONVERT(VARCHAR(10), dateadd(d,-day(dateadd(m,1,a.demandday)), dateadd(m,1, a.demandday  )),121 ) ,'-','') else b.postingdate end as postingdate,"
            strFormat = strFormat & " REPLACE(DBO.SC_GET_BUSINO_FUN(A.EXCLIENTCODE),'-','') CUSTOMERCODE, "
            strFormat = strFormat & " DBO.SC_GET_HIGHCUSTNAME_FUN(A.EXCLIENTCODE) CUSTNAME, "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then RTRIM(LTRIM(CONVERT(CHAR(17),RTRIM(LTRIM(DBO.SC_GET_HIGHCUSTNAME_FUN(A.EXCLIENTCODE))))))+'매입수수료' ELSE B.SUMM END AS SUMM,  "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then dbo.SC_GET_BA(A.DEPT_CD) else b.ba end as BA,"
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then dbo.SC_GET_CCTR(A.DEPT_CD) else b.costcenter end as costcenter,"
            strFormat = strFormat & " case when isnull(A.SUSU_AMT,0) < 0 then abs(isnull(A.SUSU_AMT,0)) else  dbo.lpad(A.SUSU_AMT,13,'0') end  amt, "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then case when isnull(A.SUSU_AMT,0) < 0 then abs(FLOOR(A.SUSU_AMT* 0.1)) else dbo.lpad(FLOOR(A.SUSU_AMT * 0.1),13,'0') end else isnull(b.vat,0) end vat,  "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then 'I0' ELSE ISNULL(SEMU,'') END AS SEMU, "
            strFormat = strFormat & " '8000' BP,"
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then CONVERT(CHAR(8),DATEADD(DD,90,replace(CONVERT(VARCHAR(10), dateadd(d,-day(dateadd(m,1,a.demandday)), dateadd(m,1, a.demandday  )),121 ) ,'-','') ),112) ELSE B.DEMANDDAY END AS DEMANDDAY, "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then CONVERT(CHAR(8),DATEADD(DD,60,A.DEMANDDAY ),112) ELSE B.DUEDATE END AS DUEDATE, "
            strFormat = strFormat & " REPLACE(DBO.SC_GET_BUSINO_FUN(A.EXCLIENTCODE),'-','') VENDOR, "
            strFormat = strFormat & " DBO.SC_GET_HIGHCUSTNAME_FUN(A.EXCLIENTCODE) VENDORNAME, "
            strFormat = strFormat & " 'O' GBN,  "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then '681180' else b.account end as  ACCOUNT, "
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then '404103' else b.DEBTOR end as  DEBTOR, "
            strFormat = strFormat & " CASE ISNULL(B.BMORDER,'') WHEN '' THEN 'AD0420' ELSE B.BMORDER END AS  BMORDER,"
            strFormat = strFormat & " CASE ISNULL(B.BANKTYPE,'') WHEN '' THEN '' ELSE B.BANKTYPE END AS  BANKTYPE,"
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then A.DEMANDDAY else b.documentdate end as documentdate,"
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then '' else b.prepayment end as  prepayment,"
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then '' else b.fromdate end as  fromdate,"
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then '' else b.todate end as  todate,"
            strFormat = strFormat & " CASE isnull(b.postingdate,'') when '' then DBO.SC_GET_HIGHCUSTNAME_FUN(A.EXCLIENTCODE)+'- 매입수수료' ELSE B.SUMMTEXT END AS SUMMTEXT ,  "
            strFormat = strFormat & " case when isnull(A.SUSU_AMT,0) < 0 then '-' else '+' end AMTGBN,"
            strFormat = strFormat & " YEARMON TAXYEARMON,  "
            strFormat = strFormat & " A.SEQ TAXNO,  "
            strFormat = strFormat & " CASE ISNULL(A.VOCHNO,'') WHEN '' THEN '' ELSE A.VOCHNO END VOCHNO,"
            strFormat = strFormat & " ISNULL(B.RMSNO,'') RMSNO, "
            strFormat = strFormat & " ISNULL(B.ERRCODE,'') ERRCODE, "
            strFormat = strFormat & " ISNULL(B.ERRMSG,'') ERRMSG,  "
            strFormat = strFormat & " ISNULL(B.PAYCODE,'') PAYCODE,"
            strFormat = strFormat & " 'YS' MEDFLAG, "
            strFormat = strFormat & " REPLACE(DBO.SC_GET_BUSINO_FUN(A.EXCLIENTCODE),'-','') REAL_MED_BUSINO, "
            strFormat = strFormat & " DBO.SC_GET_HIGHCUSTNAME_FUN(A.EXCLIENTCODE) REAL_MED_NAME, "
            strFormat = strFormat & " DENSE_RANK() OVER(ORDER BY A.EXCLIENTCODE DESC) TRANSRANK "
            strFormat = strFormat & " FROM MD_IFCMALL_SUSU A LEFT JOIN MD_TRUVOCH_MST B   "
            strFormat = strFormat & " ON A.YEARMON = B.TAXYEARMON AND A.SEQ = B.TAXNO And B.GBN = 'O' and b.medflag = 'YS'"
            strFormat = strFormat & " WHERE 1=1 and isnull(CONFIRM_USER,'') <> '' and  isnull(SUSU_AMT,0) <> 0 "
            strFormat = strFormat & " {0} "
            strFormat = strFormat & " order by DBO.SC_GET_HIGHCUSTNAME_FUN(A.EXCLIENTCODE)"

            strSQL = String.Format(strFormat, strWhere)

            '데이터 조회
            Try
                .mobjSCGLSql.SQLConnect(.DBConnStr)
                vntData = .mobjSCGLSql.SQLSelectArr(strSQL, intRowCnt, intColCnt, , True)
                Return vntData
            Catch err As Exception
                Throw RaiseSysErr(err, CLASS_NAME & ".SelectRtn_OUTSUSU")
            Finally
                .mobjSCGLSql.SQLDisconnect()
            End Try
        End With
    End Function

    Public Function ProcessRtn(ByVal strInfoXML As String, _
                               ByVal vntData As Object, _
                               ByVal strRETURNLIST As String, _
                               ByVal strGBN As String, _
                               ByVal strMEDFLAG As String) As Integer

        Dim intRtn As Integer
        Dim i, intColCnt, intRows As Integer
        Dim strPOSTINGDATE As String
        Dim strDEMANDDAY As String
        Dim strDOCUMENTDATE As String
        Dim strDUEDATE As String
        Dim strFROMDATE As String
        Dim strTODATE As String
        Dim strATTR01

        SetConfig(strInfoXML)
        With mobjSCGLConfig
            Try
                .mobjSCGLSql.SQLConnect(.DBConnStr)
                .mobjSCGLSql.SQLBeginTrans()

                If IsArray(vntData) Then

                    '''사용할 Entity 개체생성(Config 정보를 넘겨생성)
                    mobjceMD_TRUVOCH_MST = New ceMD_TRUVOCH_MST(mobjSCGLConfig)
                    intColCnt = UBound(vntData, 1) : intRows = UBound(vntData, 2)

                    strATTR01 = ""
                    '''해당하는Row 만큼 Loop

                    If strMEDFLAG = "Y" Then
                        For i = 1 To intRows
                            If GetElement(vntData, "POSTINGDATE", intColCnt, i, OPTIONAL_STR) <> "" Then strPOSTINGDATE = Replace(GetElement(vntData, "POSTINGDATE", intColCnt, i, OPTIONAL_STR), "-", "")
                            If GetElement(vntData, "DEMANDDAY", intColCnt, i, OPTIONAL_STR) <> "" Then strDEMANDDAY = Replace(GetElement(vntData, "DEMANDDAY", intColCnt, i, OPTIONAL_STR), "-", "")
                            If GetElement(vntData, "DOCUMENTDATE", intColCnt, i, OPTIONAL_STR) <> "" Then strDOCUMENTDATE = Replace(GetElement(vntData, "DOCUMENTDATE", intColCnt, i, OPTIONAL_STR), "-", "")
                            If GetElement(vntData, "FROMDATE", intColCnt, i, OPTIONAL_STR) <> "" Then strFROMDATE = Replace(GetElement(vntData, "FROMDATE", intColCnt, i, OPTIONAL_STR), "-", "")
                            If GetElement(vntData, "TODATE", intColCnt, i, OPTIONAL_STR) <> "" Then strTODATE = Replace(GetElement(vntData, "TODATE", intColCnt, i, OPTIONAL_STR), "-", "")
                            If GetElement(vntData, "DUEDATE", intColCnt, i, OPTIONAL_STR) <> "" Then strDUEDATE = Replace(GetElement(vntData, "DUEDATE", intColCnt, i, OPTIONAL_STR), "-", "")
                            strATTR01 = ""
                            If Trim(GetElement(vntData, "DEBTOR", intColCnt, i, OPTIONAL_STR)) = "" Then
                                strATTR01 = "0"
                            Else
                                strATTR01 = ""
                            End If
                            intRtn = InsertRtn(vntData, intColCnt, i, strPOSTINGDATE, strDEMANDDAY, strFROMDATE, strTODATE, strDOCUMENTDATE, strDUEDATE, strMEDFLAG, strATTR01)
                        Next
                    ElseIf strMEDFLAG = "YO" Then
                        For i = 1 To intRows
                            If GetElement(vntData, "CHK", intColCnt, i) = "1" Then
                                If GetElement(vntData, "POSTINGDATE", intColCnt, i, OPTIONAL_STR) <> "" Then strPOSTINGDATE = Replace(GetElement(vntData, "POSTINGDATE", intColCnt, i, OPTIONAL_STR), "-", "")
                                If GetElement(vntData, "DEMANDDAY", intColCnt, i, OPTIONAL_STR) <> "" Then strDEMANDDAY = Replace(GetElement(vntData, "DEMANDDAY", intColCnt, i, OPTIONAL_STR), "-", "")
                                If GetElement(vntData, "DOCUMENTDATE", intColCnt, i, OPTIONAL_STR) <> "" Then strDOCUMENTDATE = Replace(GetElement(vntData, "DOCUMENTDATE", intColCnt, i, OPTIONAL_STR), "-", "")
                                If GetElement(vntData, "FROMDATE", intColCnt, i, OPTIONAL_STR) <> "" Then strFROMDATE = Replace(GetElement(vntData, "FROMDATE", intColCnt, i, OPTIONAL_STR), "-", "")
                                If GetElement(vntData, "TODATE", intColCnt, i, OPTIONAL_STR) <> "" Then strTODATE = Replace(GetElement(vntData, "TODATE", intColCnt, i, OPTIONAL_STR), "-", "")
                                If GetElement(vntData, "DUEDATE", intColCnt, i, OPTIONAL_STR) <> "" Then strDUEDATE = Replace(GetElement(vntData, "DUEDATE", intColCnt, i, OPTIONAL_STR), "-", "")

                                intRtn = InsertRtn(vntData, intColCnt, i, strPOSTINGDATE, strDEMANDDAY, strFROMDATE, strTODATE, strDOCUMENTDATE, strDUEDATE, strMEDFLAG, strATTR01)
                            End If
                        Next
                    ElseIf strMEDFLAG = "YO2" Then
                        For i = 1 To intRows
                            If GetElement(vntData, "CHK", intColCnt, i) = "1" Then
                                If GetElement(vntData, "POSTINGDATE", intColCnt, i, OPTIONAL_STR) <> "" Then strPOSTINGDATE = Replace(GetElement(vntData, "POSTINGDATE", intColCnt, i, OPTIONAL_STR), "-", "")
                                If GetElement(vntData, "DEMANDDAY", intColCnt, i, OPTIONAL_STR) <> "" Then strDEMANDDAY = Replace(GetElement(vntData, "DEMANDDAY", intColCnt, i, OPTIONAL_STR), "-", "")
                                If GetElement(vntData, "DOCUMENTDATE", intColCnt, i, OPTIONAL_STR) <> "" Then strDOCUMENTDATE = Replace(GetElement(vntData, "DOCUMENTDATE", intColCnt, i, OPTIONAL_STR), "-", "")
                                If GetElement(vntData, "FROMDATE", intColCnt, i, OPTIONAL_STR) <> "" Then strFROMDATE = Replace(GetElement(vntData, "FROMDATE", intColCnt, i, OPTIONAL_STR), "-", "")
                                If GetElement(vntData, "TODATE", intColCnt, i, OPTIONAL_STR) <> "" Then strTODATE = Replace(GetElement(vntData, "TODATE", intColCnt, i, OPTIONAL_STR), "-", "")
                                If GetElement(vntData, "DUEDATE", intColCnt, i, OPTIONAL_STR) <> "" Then strDUEDATE = Replace(GetElement(vntData, "DUEDATE", intColCnt, i, OPTIONAL_STR), "-", "")

                                intRtn = InsertRtn(vntData, intColCnt, i, strPOSTINGDATE, strDEMANDDAY, strFROMDATE, strTODATE, strDOCUMENTDATE, strDUEDATE, strMEDFLAG, strATTR01)
                            End If
                        Next
                    ElseIf strMEDFLAG = "YO3" Then
                        For i = 1 To intRows
                            If GetElement(vntData, "POSTINGDATE", intColCnt, i, OPTIONAL_STR) <> "" Then strPOSTINGDATE = Replace(GetElement(vntData, "POSTINGDATE", intColCnt, i, OPTIONAL_STR), "-", "")
                            If GetElement(vntData, "DEMANDDAY", intColCnt, i, OPTIONAL_STR) <> "" Then strDEMANDDAY = Replace(GetElement(vntData, "DEMANDDAY", intColCnt, i, OPTIONAL_STR), "-", "")
                            If GetElement(vntData, "DOCUMENTDATE", intColCnt, i, OPTIONAL_STR) <> "" Then strDOCUMENTDATE = Replace(GetElement(vntData, "DOCUMENTDATE", intColCnt, i, OPTIONAL_STR), "-", "")
                            If GetElement(vntData, "FROMDATE", intColCnt, i, OPTIONAL_STR) <> "" Then strFROMDATE = Replace(GetElement(vntData, "FROMDATE", intColCnt, i, OPTIONAL_STR), "-", "")
                            If GetElement(vntData, "TODATE", intColCnt, i, OPTIONAL_STR) <> "" Then strTODATE = Replace(GetElement(vntData, "TODATE", intColCnt, i, OPTIONAL_STR), "-", "")
                            If GetElement(vntData, "DUEDATE", intColCnt, i, OPTIONAL_STR) <> "" Then strDUEDATE = Replace(GetElement(vntData, "DUEDATE", intColCnt, i, OPTIONAL_STR), "-", "")
                            strATTR01 = ""
                            If Trim(GetElement(vntData, "DEBTOR", intColCnt, i, OPTIONAL_STR)) = "" Then
                                strATTR01 = "0"
                            Else
                                strATTR01 = ""
                            End If
                            intRtn = InsertRtn(vntData, intColCnt, i, strPOSTINGDATE, strDEMANDDAY, strFROMDATE, strTODATE, strDOCUMENTDATE, strDUEDATE, strMEDFLAG, strATTR01)
                        Next
                    ElseIf strMEDFLAG = "YS" Then
                        For i = 1 To intRows
                            If GetElement(vntData, "CHK", intColCnt, i) = "1" Then
                                If GetElement(vntData, "POSTINGDATE", intColCnt, i, OPTIONAL_STR) <> "" Then strPOSTINGDATE = Replace(GetElement(vntData, "POSTINGDATE", intColCnt, i, OPTIONAL_STR), "-", "")
                                If GetElement(vntData, "DEMANDDAY", intColCnt, i, OPTIONAL_STR) <> "" Then strDEMANDDAY = Replace(GetElement(vntData, "DEMANDDAY", intColCnt, i, OPTIONAL_STR), "-", "")
                                If GetElement(vntData, "DOCUMENTDATE", intColCnt, i, OPTIONAL_STR) <> "" Then strDOCUMENTDATE = Replace(GetElement(vntData, "DOCUMENTDATE", intColCnt, i, OPTIONAL_STR), "-", "")
                                If GetElement(vntData, "FROMDATE", intColCnt, i, OPTIONAL_STR) <> "" Then strFROMDATE = Replace(GetElement(vntData, "FROMDATE", intColCnt, i, OPTIONAL_STR), "-", "")
                                If GetElement(vntData, "TODATE", intColCnt, i, OPTIONAL_STR) <> "" Then strTODATE = Replace(GetElement(vntData, "TODATE", intColCnt, i, OPTIONAL_STR), "-", "")
                                If GetElement(vntData, "DUEDATE", intColCnt, i, OPTIONAL_STR) <> "" Then strDUEDATE = Replace(GetElement(vntData, "DUEDATE", intColCnt, i, OPTIONAL_STR), "-", "")

                                intRtn = InsertRtn(vntData, intColCnt, i, strPOSTINGDATE, strDEMANDDAY, strFROMDATE, strTODATE, strDOCUMENTDATE, strDUEDATE, strMEDFLAG, strATTR01)
                            End If
                        Next
                    End If

                    intRtn = UpdateRtn(strInfoXML, strRETURNLIST, strGBN, strMEDFLAG)
                End If
                .mobjSCGLSql.SQLCommitTrans()
                Return intRtn
            Catch err As Exception
                .mobjSCGLSql.SQLRollbackTrans()
                Throw RaiseSysErr(err, CLASS_NAME & ".ProcessRtn")
            Finally
                .mobjSCGLSql.SQLDisconnect()
                mobjceMD_TRUVOCH_MST.Dispose()
            End Try
        End With
    End Function

    Public Function UpdateRtn(ByVal strInfoXML As String, _
                              ByVal strRETURNLIST As String, _
                              ByVal strGBN As String, _
                              ByVal strMEDFLAG As String) As Integer

        Dim intRtn As Integer
        Dim i, intColCnt, intRows As Integer
        Dim firstArray
        Dim secondArray

        Dim strTAXYEARMON As String
        Dim intTAXNO As Integer
        Dim strDOC_STATUS As String
        Dim strDOC_MESSAGE As String
        Dim strVOCHNO As String
        Dim strIF_KEY As String

        SetConfig(strInfoXML)
        With mobjSCGLConfig
            Try
                firstArray = Split(strRETURNLIST, ":", -1, CompareMethod.Text)
                If strRETURNLIST <> "" Then

                    '''사용할 Entity 개체생성(Config 정보를 넘겨생성)
                    mobjceMD_TRUVOCH_MST = New ceMD_TRUVOCH_MST(mobjSCGLConfig)

                    For i = 0 To firstArray.length - 1
                        secondArray = Split(firstArray(i), "|", -1, CompareMethod.Text)

                        strTAXYEARMON = secondArray(0)
                        intTAXNO = CInt(secondArray(1))
                        strDOC_STATUS = secondArray(2)
                        strDOC_MESSAGE = Replace(secondArray(3), "'", "''")
                        strVOCHNO = secondArray(4)
                        strIF_KEY = secondArray(5)

                        If strDOC_STATUS = "S" Then
                            strDOC_STATUS = ""
                            strDOC_MESSAGE = ""
                        Else
                            strDOC_STATUS = "1"
                        End If

                        intRtn = UpdateRtn_VOCHNO(strGBN, strMEDFLAG, strTAXYEARMON, intTAXNO, strDOC_STATUS, strDOC_MESSAGE, strVOCHNO, strIF_KEY)
                    Next
                End If
                Return intRtn
            Catch err As Exception
                Throw RaiseSysErr(err, CLASS_NAME & ".UpdateRtn")
            Finally
                mobjceMD_TRUVOCH_MST.Dispose()
            End Try
        End With
    End Function

    Public Function VOCHDELL(ByVal strInfoXML As String, _
                             ByVal strRETURNLIST As String, _
                             ByVal strGBN As String, _
                             ByVal strMEDFLAG As String) As Integer
        Dim intRtn As Integer

        Dim i, intColCnt, intRows As Integer
        Dim firstArray
        Dim secondArray
        Dim strTAXYEARMON As String
        Dim intTAXNO As Integer
        Dim strDOC_STATUS As String
        Dim strDOC_MESSAGE As String
        Dim strVOCHNO As String

        SetConfig(strInfoXML)
        With mobjSCGLConfig
            Try
                .mobjSCGLSql.SQLConnect(.DBConnStr)
                .mobjSCGLSql.SQLBeginTrans()

                firstArray = Split(strRETURNLIST, ":", -1, CompareMethod.Text)

                If strRETURNLIST <> "" Then
                    '''사용할 Entity 개체생성(Config 정보를 넘겨생성)
                    mobjceMD_TRUVOCH_MST = New ceMD_TRUVOCH_MST(mobjSCGLConfig)

                    For i = 0 To firstArray.length - 1
                        secondArray = Split(firstArray(i), "|", -1, CompareMethod.Text)

                        strTAXYEARMON = secondArray(0)
                        intTAXNO = CInt(secondArray(1))
                        strDOC_STATUS = secondArray(2)
                        strDOC_MESSAGE = Replace(secondArray(3), "'", "''")
                        strVOCHNO = secondArray(4)

                        If strDOC_STATUS = "S" Then
                            '전표내역 삭제
                            intRtn = mobjceMD_TRUVOCH_MST.Delete_vochno(strTAXYEARMON, strVOCHNO, strGBN)

                            '세금계산서의 전표번호 '' 로 업데이트 'strGBN = "O"면 하지않음(옥외일경우만 해당)
                            If strGBN = "S" Or strGBN = "YO3" Then
                                intRtn = mobjceMD_TRUVOCH_MST.UpdateDelete(strTAXYEARMON, intTAXNO, strMEDFLAG, strGBN)
                            End If

                            '신탁의 전표번호 ''로 업데이트
                            intRtn = mobjceMD_TRUVOCH_MST.Update_vochno(strTAXYEARMON, intTAXNO, strMEDFLAG, strGBN)
                        ElseIf strDOC_STATUS = "E" Then
                            '전표삭제시 SAP오류는 오류전표로 보내기위함.
                            strDOC_STATUS = "1"
                            intRtn = mobjceMD_TRUVOCH_MST.UpdateRtn_DELETEERR(strGBN, strTAXYEARMON, intTAXNO, strDOC_STATUS, strDOC_MESSAGE, strVOCHNO)
                        End If
                    Next
                End If

                .mobjSCGLSql.SQLCommitTrans()
                Return intRtn
            Catch err As Exception
                .mobjSCGLSql.SQLRollbackTrans()
                Throw RaiseSysErr(err, CLASS_NAME & ".VOCHDELL")
            Finally
                .mobjSCGLSql.SQLDisconnect()
                mobjceMD_TRUVOCH_MST.Dispose()

            End Try
        End With
    End Function

    Public Function VOCHDELL_BUY(ByVal strInfoXML As String, _
                                 ByVal strRETURNLIST As String, _
                                 ByVal strGBN As String, _
                                 ByVal strMEDFLAG As String, _
                                 ByVal strFLAG As String) As Integer
        Dim intRtn As Integer

        Dim i, intColCnt, intRows As Integer
        Dim firstArray
        Dim secondArray
        Dim strTAXYEARMON As String
        Dim intTAXNO As Integer
        Dim strDOC_STATUS As String
        Dim strDOC_MESSAGE As String
        Dim strVOCHNO As String

        SetConfig(strInfoXML)
        With mobjSCGLConfig
            Try
                .mobjSCGLSql.SQLConnect(.DBConnStr)
                .mobjSCGLSql.SQLBeginTrans()

                firstArray = Split(strRETURNLIST, ":", -1, CompareMethod.Text)
                If strRETURNLIST <> "" Then
                    '''사용할 Entity 개체생성(Config 정보를 넘겨생성)
                    mobjceMD_TRUVOCH_MST = New ceMD_TRUVOCH_MST(mobjSCGLConfig)

                    For i = 0 To firstArray.length - 1
                        secondArray = Split(firstArray(i), "|", -1, CompareMethod.Text)

                        strTAXYEARMON = secondArray(0)
                        intTAXNO = CInt(secondArray(1))
                        strDOC_STATUS = secondArray(2)
                        strDOC_MESSAGE = Replace(secondArray(3), "'", "''")
                        strVOCHNO = secondArray(4)

                        If strDOC_STATUS = "S" Then
                            '전표내역 삭제
                            intRtn = mobjceMD_TRUVOCH_MST.Delete_vochno_BUY(strTAXYEARMON, strVOCHNO, strGBN, strFLAG)
                            '세금계산서의 전표번호 '' 로 업데이트
                            intRtn = mobjceMD_TRUVOCH_MST.UpdateDelete_BUY(strTAXYEARMON, intTAXNO, strMEDFLAG, strGBN, strFLAG)
                        ElseIf strDOC_STATUS = "E" Then
                            '전표삭제시 SAP오류는 오류전표로 보내기위함.
                            strDOC_STATUS = "1"
                            intRtn = mobjceMD_TRUVOCH_MST.UpdateRtn_DELETEERR_BUY(strGBN, strTAXYEARMON, intTAXNO, strDOC_STATUS, strDOC_MESSAGE, strVOCHNO)
                        End If
                    Next
                End If

                .mobjSCGLSql.SQLCommitTrans()
                Return intRtn
            Catch err As Exception
                .mobjSCGLSql.SQLRollbackTrans()
                Throw RaiseSysErr(err, CLASS_NAME & ".VOCHDELL_BUY")
            Finally
                .mobjSCGLSql.SQLDisconnect()
                mobjceMD_TRUVOCH_MST.Dispose()
            End Try
        End With
    End Function

    Public Function DeleteRtn(ByVal strInfoXML As String, _
                              ByVal vntData As Object) As Integer
        Dim intRtn As Integer
        Dim i, intColCnt, intRows As Integer

        SetConfig(strInfoXML)
        With mobjSCGLConfig
            Try
                .mobjSCGLSql.SQLConnect(.DBConnStr)
                .mobjSCGLSql.SQLBeginTrans()

                If IsArray(vntData) Then

                    '''사용할 Entity 개체생성(Config 정보를 넘겨생성)
                    mobjceMD_TRUVOCH_MST = New ceMD_TRUVOCH_MST(mobjSCGLConfig)
                    intColCnt = UBound(vntData, 1) : intRows = UBound(vntData, 2)

                    '''해당하는Row 만큼 Loop
                    For i = 1 To intRows
                        If Trim(GetElement(vntData, "CHK", intColCnt, i)) = "" Then
                        Else
                            If GetElement(vntData, "CHK", intColCnt, i) = 1 And GetElement(vntData, "ERRCODE", intColCnt, i) = 1 Then
                                intRtn = DeleteRtn_ERR(vntData, intColCnt, i)
                            End If
                        End If
                    Next
                End If
                .mobjSCGLSql.SQLCommitTrans()
                Return intRtn
            Catch err As Exception
                .mobjSCGLSql.SQLRollbackTrans()
                Throw RaiseSysErr(err, CLASS_NAME & ".DeleteRtn")
            Finally
                .mobjSCGLSql.SQLDisconnect()
                mobjceMD_TRUVOCH_MST.Dispose()
            End Try
        End With
    End Function

    Public Function DeleteRtn_GANG(ByVal strInfoXML As String, _
                                   ByVal strTAXYEARMON As String, _
                                   ByVal intTAXNO As String, _
                                   ByVal strVOCHNO As String, _
                                   ByVal strGBN As String, _
                                   ByVal strMEDFLAG As String) As Integer
        Dim intRtn As Integer
        Dim i, intColCnt, intRows As Integer

        SetConfig(strInfoXML)
        With mobjSCGLConfig
            Try
                .mobjSCGLSql.SQLConnect(.DBConnStr)
                .mobjSCGLSql.SQLBeginTrans()

                '''사용할 Entity 개체생성(Config 정보를 넘겨생성)
                mobjceMD_TRUVOCH_MST = New ceMD_TRUVOCH_MST(mobjSCGLConfig)

                intRtn = mobjceMD_TRUVOCH_MST.Delete_vochno(strTAXYEARMON, strVOCHNO, strGBN)
                '세금계산서의 전표번호 '' 로 업데이트
                If strGBN = "S" Then
                    intRtn = mobjceMD_TRUVOCH_MST.UpdateDelete(strTAXYEARMON, intTAXNO, strMEDFLAG, strGBN)
                End If

                '신탁의 전표번호 ''로 업데이트
                intRtn = mobjceMD_TRUVOCH_MST.Update_vochno(strTAXYEARMON, intTAXNO, strMEDFLAG, strGBN)

                .mobjSCGLSql.SQLCommitTrans()
                Return intRtn
            Catch err As Exception
                .mobjSCGLSql.SQLRollbackTrans()
                Throw RaiseSysErr(err, CLASS_NAME & ".DeleteRtn_GANG")
            Finally
                .mobjSCGLSql.SQLDisconnect()
                mobjceMD_TRUVOCH_MST.Dispose()
            End Try
        End With
    End Function

    Public Function DeleteRtn_GANG_BUY(ByVal strInfoXML As String, _
                                       ByVal strTAXYEARMON As String, _
                                       ByVal intTAXNO As String, _
                                       ByVal strVOCHNO As String, _
                                       ByVal strGBN As String, _
                                       ByVal strMEDFLAG As String, _
                                       ByVal strFLAG As String) As Integer
        Dim intRtn As Integer
        Dim i, intColCnt, intRows As Integer

        SetConfig(strInfoXML)
        With mobjSCGLConfig
            Try
                .mobjSCGLSql.SQLConnect(.DBConnStr)
                .mobjSCGLSql.SQLBeginTrans()

                '''사용할 Entity 개체생성(Config 정보를 넘겨생성)
                mobjceMD_TRUVOCH_MST = New ceMD_TRUVOCH_MST(mobjSCGLConfig)

                intRtn = mobjceMD_TRUVOCH_MST.Delete_vochno_BUY(strTAXYEARMON, strVOCHNO, strGBN, strFLAG)
                '세금계산서의 전표번호 '' 로 업데이트
                intRtn = mobjceMD_TRUVOCH_MST.UpdateDelete_BUY(strTAXYEARMON, intTAXNO, strMEDFLAG, strGBN, strFLAG)

                .mobjSCGLSql.SQLCommitTrans()
                Return intRtn
            Catch err As Exception
                .mobjSCGLSql.SQLRollbackTrans()
                Throw RaiseSysErr(err, CLASS_NAME & ".DeleteRtn_GANG")
            Finally
                .mobjSCGLSql.SQLDisconnect()
                mobjceMD_TRUVOCH_MST.Dispose()
            End Try
        End With
    End Function
#End Region

#Region "GROUP BLOCK : 외부에 비공개 Method"
    Private Function InsertRtn(ByVal vntData As Object, _
                               ByVal intColCnt As Integer, _
                               ByVal intRow As Integer, _
                               ByVal strPOSTINGDATE As String, _
                               ByVal strDEMANDDAY As String, _
                               ByVal strFROMDATE As String, _
                               ByVal strTODATE As String, _
                               ByVal strDOCUMENTDATE As String, _
                               ByVal strDUEDATE As String, _
                               ByVal strMEDFLAG As String, _
                               ByVal strATTR01 As String) As Integer

        Dim intRtn As Integer
        intRtn = mobjceMD_TRUVOCH_MST.InsertDo( _
                                       strPOSTINGDATE, _
                                       GetElement(vntData, "CUSTOMERCODE", intColCnt, intRow), _
                                       GetElement(vntData, "SUMM", intColCnt, intRow), _
                                       GetElement(vntData, "BA", intColCnt, intRow), _
                                       GetElement(vntData, "COSTCENTER", intColCnt, intRow), _
                                       GetElement(vntData, "AMT", intColCnt, intRow, NULL_NUM, True), _
                                       GetElement(vntData, "VAT", intColCnt, intRow, NULL_NUM, True), _
                                       GetElement(vntData, "SEMU", intColCnt, intRow), _
                                       GetElement(vntData, "BP", intColCnt, intRow), _
                                       strDEMANDDAY, _
                                       strDUEDATE, _
                                       GetElement(vntData, "VENDOR", intColCnt, intRow), _
                                       GetElement(vntData, "TAXYEARMON", intColCnt, intRow), _
                                       GetElement(vntData, "TAXNO", intColCnt, intRow, NULL_NUM, True), _
                                       GetElement(vntData, "GBN", intColCnt, intRow), _
                                       GetElement(vntData, "DEBTOR", intColCnt, intRow), _
                                       GetElement(vntData, "ACCOUNT", intColCnt, intRow), _
                                       strDOCUMENTDATE, _
                                       GetElement(vntData, "PAYCODE", intColCnt, intRow), _
                                       GetElement(vntData, "BANKTYPE", intColCnt, intRow), _
                                       GetElement(vntData, "BMORDER", intColCnt, intRow), _
                                       GetElement(vntData, "PREPAYMENT", intColCnt, intRow), _
                                       strFROMDATE, _
                                       strTODATE, _
                                       GetElement(vntData, "SUMMTEXT", intColCnt, intRow), _
                                       GetElement(vntData, "GFLAG", intColCnt, intRow), _
                                       "", _
                                       "", _
                                       strMEDFLAG, _
                                       strATTR01)

        Return intRtn
    End Function

    Private Function UpdateRtn_VOCHNO(ByVal strGBN As String, _
                                      ByVal strMEDFLAG As String, _
                                      ByVal strTAXYEARMON As String, _
                                      ByVal intTAXNO As Integer, _
                                      ByVal strDOC_STATUS As String, _
                                      ByVal strDOC_MESSAGE As String, _
                                      ByVal strVOCHNO As String, _
                                      ByVal strIF_KEY As String) As Integer

        Dim intRtn As Integer

        intRtn = mobjceMD_TRUVOCH_MST.UpdateRtn_VOCHNO( _
                                       strGBN, _
                                       strMEDFLAG, _
                                       strTAXYEARMON, _
                                       intTAXNO, _
                                       strDOC_STATUS, _
                                       strDOC_MESSAGE, _
                                       strVOCHNO, _
                                       strIF_KEY)

        Return intRtn
    End Function

    Private Function DeleteRtn_ERR(ByVal vntData As Object, _
                                     ByVal intColCnt As Integer, _
                                     ByVal intRow As Integer) As Integer

        Dim intRtn As Integer
        intRtn = mobjceMD_TRUVOCH_MST.Delete_ERR(GetElement(vntData, "TAXYEARMON", intColCnt, intRow), _
                                                 GetElement(vntData, "TAXNO", intColCnt, intRow), _
                                                 GetElement(vntData, "GBN", intColCnt, intRow), _
                                                 GetElement(vntData, "MEDFLAG", intColCnt, intRow))

        Return intRtn
    End Function
#End Region
End Class
