'****************************************************************************************
'Generated By: MakeSFAR V.2.0.0 - 컨트롤 클래스 메이커
'시스템구분    : 솔루션명 /시스템명/Server Control Class
'실행   환경    : COM+ Service Server Package
'프로그램명    : ccMDCMDEPTMST.vb
'기         능    : - 기능을 명시 합니다.
'특이  사항     : - 특이사항에 대해 표현
'                     -
'----------------------------------------------------------------------------------------
'HISTORY    :1) 
'            2) 
'****************************************************************************************

Imports System.Xml                  ' XML처리
Imports SCGLControl                 ' ControlClass의 Base Class
Imports SCGLUtil.cbSCGLConfig       ' ConfigurationClass
Imports SCGLUtil.cbSCGLErr          '오류처리 클래스
Imports SCGLUtil.cbSCGLXml          'XML처리 클래스
Imports SCGLUtil.cbSCGLUtil         '기타유틸리티 클래스
Imports eSCCO                       '엔터티 추가

' 엔티티 클래스 사용시 해당 엔티티 클래스의 프로젝트를 참조한 후 Imports 하십시요. 
' Imports 엔티티프로젝트

Public Class ccSCCODEPTMST
    Inherits ccControl
#Region "GROUP BLOCK : 전역 또는 모듈레벨의 변수/상수 선언"
    Private CLASS_NAME = "ccSCCODEPTMST"                  '자신의 클래스명
    Private mobjceSC_CC As eSCCO.ceSC_CC         '사용할 Entity 변수 선언

#End Region

#Region "GROUP BLOCK : 외부에비공개"
    Public Function GetCC(ByVal strInfoXML As String, _
                          ByRef intRowCnt As Integer, ByRef intColCnt As Integer, _
                          ByVal strCODE As String, _
                          ByVal strNAME As String, _
                          ByVal strYN As String) As Object
        Dim strSQL, strFormat, strSelFields, strKeys As String
        Dim strCondition As String
        Dim strCondition2 As String
        Dim strChkDate As String = ""
        Dim vntData As Object
        Dim Con1, Con2, Con3 As String
        Con1 = ""
        Con2 = ""
        Con3 = ""

        SetConfig(strInfoXML)   '기본정보 설정
        With mobjSCGLConfig


            '한글인 경우
            If strCODE <> "" Then Con1 = String.Format(" AND (CC_CODE = '{0}')", strCODE)
            If strNAME <> "" Then Con2 = String.Format(" AND CC_NAME LIKE '%{0}%'", strNAME)
            If strYN <> "A" Then Con3 = String.Format(" AND USE_YN = '{0}'", strYN)

            '조회 필드 설정

            strSelFields = "CC_CODE,CC_NAME,OC_CODE,OC_NAME,CASE USE_YN WHEN 'Y' THEN '1' ELSE '0' END AS USE_YN,SDATE,EDATE"




            strFormat = "SELECT {0} FROM SC_CC A " & _
                                     "WHERE 1=1 AND PC='Y'   {1} {2} {3} " & _
                                     "ORDER BY CC_CODE"
            strSQL = String.Format(strFormat, _
                                   strSelFields, Con1, Con2, Con3)




            '데이터 조회
            Try
                .mobjSCGLSql.SQLConnect(.DBConnStr)
                vntData = .mobjSCGLSql.SQLSelectArr(strSQL, intRowCnt, intColCnt, , True)
                Return vntData
            Catch err As Exception
                Throw RaiseSysErr(err, CLASS_NAME & ".GetCC")
            Finally
                .mobjSCGLSql.SQLDisconnect()
            End Try
        End With
    End Function
    Public Function GetDup(ByVal strInfoXML As String, _
                           ByRef intRowCnt As Integer, ByRef intColCnt As Integer, _
                           ByVal strUSEYN As String, _
                           ByVal strCC As String) As Object
        Dim strSQL, strFormat, strSelFields, strKeys As String
        Dim strCondition As String
        Dim strCondition2 As String
        Dim strChkDate As String = ""
        Dim vntData As Object


        SetConfig(strInfoXML)   '기본정보 설정
        With mobjSCGLConfig



            '조회 필드 설정

            strSQL = "SELECT CC_CODE FROM SC_CC WHERE CC_CODE = '" & strCC & "' AND USE_YN = '" & strUSEYN & "'"


            '데이터 조회
            Try
                .mobjSCGLSql.SQLConnect(.DBConnStr)
                vntData = .mobjSCGLSql.SQLSelectArr(strSQL, intRowCnt, intColCnt, , True)
                Return vntData
            Catch err As Exception
                Throw RaiseSysErr(err, CLASS_NAME & ".GetDup")
            Finally
                .mobjSCGLSql.SQLDisconnect()
            End Try
        End With
    End Function
    Public Function ProcessRtn(ByVal strInfoXML As String, _
                               ByVal vntData As Object) As Integer
        Dim intRtn As Integer
        Dim i, intColCnt, intRows As Integer
        Dim dblID As Double '자동 ID 를사용할 때만 사용
        Dim strUSEYN As String
        SetConfig(strInfoXML)
        Dim strSDATE As String
        Dim strEDATE As String
        With mobjSCGLConfig
            Try
                .mobjSCGLSql.SQLConnect(.DBConnStr)
                .mobjSCGLSql.SQLBeginTrans()

                If IsArray(vntData) Then
                    '''사용할 Entity 개체생성(Config 정보를 넘겨생성)
                    mobjceSC_CC = New ceSC_CC(mobjSCGLConfig)
                    '''vntData의 컬럼수, 로우수를 변수입력
                    intColCnt = UBound(vntData, 1) : intRows = UBound(vntData, 2)
                    '''해당하는Row 만큼 Loop
                    strUSEYN = ""
                    strSDATE = ""
                    strEDATE = ""


                    For i = 1 To intRows
                        If GetElement(vntData, "USE_YN", intColCnt, i) = "1" Then
                            strUSEYN = "Y"
                        ElseIf GetElement(vntData, "USE_YN", intColCnt, i) = "0" Then
                            strUSEYN = "N"
                        End If
                        If GetElement(vntData, "SDATE", intColCnt, i, OPTIONAL_STR) <> "" Then strSDATE = GetElement(vntData, "SDATE", intColCnt, i, OPTIONAL_STR).SUBSTRING(0, 4) & GetElement(vntData, "SDATE", intColCnt, i, OPTIONAL_STR).SUBSTRING(5, 2) & GetElement(vntData, "SDATE", intColCnt, i, OPTIONAL_STR).SUBSTRING(8, 2)
                        If GetElement(vntData, "EDATE", intColCnt, i, OPTIONAL_STR) <> "" Then strEDATE = GetElement(vntData, "EDATE", intColCnt, i, OPTIONAL_STR).SUBSTRING(0, 4) & GetElement(vntData, "EDATE", intColCnt, i, OPTIONAL_STR).SUBSTRING(5, 2) & GetElement(vntData, "EDATE", intColCnt, i, OPTIONAL_STR).SUBSTRING(8, 2)
                        intRtn = UpdateRtn(vntData, intColCnt, i, strUSEYN, strSDATE, strEDATE)

                    Next
                End If
                .mobjSCGLSql.SQLCommitTrans()
                Return intRows
            Catch err As Exception
                .mobjSCGLSql.SQLRollbackTrans()
                Throw RaiseSysErr(err, CLASS_NAME & ".ProcessRtn")
            Finally
                .mobjSCGLSql.SQLDisconnect()
                mobjceSC_CC.Dispose()

            End Try
        End With
    End Function
#End Region
#Region "GROUP BLOCK : 외부에 비공개 Method"
    Private Function UpdateRtn(ByVal vntData As Object, _
                               ByVal intColCnt As Integer, _
                               ByVal intRow As Integer, _
                               ByVal strUSEYN As String, _
                               ByVal strSDATE As String, _
                               ByVal strEDATE As String) As Integer
        Dim intRtn As Integer
        'CC_CODE|CC_NAME|OC_CODE|BTN|OC_NAME|USE_YN|SDATE|EDATE
        intRtn = mobjceSC_CC.UpdateDo( _
                                       GetElement(vntData, "CC_CODE", intColCnt, intRow), _
                                       GetElement(vntData, "OC_CODE", intColCnt, intRow), _
                                       strUSEYN, _
                                       strSDATE, _
                                       strEDATE)
        Return intRtn
    End Function
#End Region
End Class
